# Design System, UX/UI e SEO — MeuSiteCasamento

> Tokens visuais, componentes do Design System (`components/ui/`), princípios de UX, responsividade, acessibilidade e SEO. Para regras de código/organização de componentes (onde um componente deve viver, como ele recebe dados), ver [`CLAUDE.md`](../CLAUDE.md), seção "Convenções de Código". Em caso de conflito, CLAUDE.md prevalece.

---

## 1. Experiência do Usuário (UX)

### 1.1 Princípios gerais

- **Fricção mínima para o convidado**: nenhuma criação de conta é exigida para RSVP ou reserva de presente.
- **Clareza de estado**: em qualquer tela, o usuário deve entender imediatamente "o que já foi feito" e "o que falta fazer" (ex: RSVP já enviado vs. pendente).
- **Feedback imediato**: toda ação de mutação (confirmar presença, reservar presente) mostra feedback otimista ou loading state claro, seguido de confirmação visual (toast/inline).
- **Prevenção de erro antes de correção de erro**: validações client-side (Zod + VeeValidate) bloqueiam submissões inválidas antes de chegar ao servidor, mas o servidor sempre revalida (nunca confia apenas no client).

### 1.2 Fluxos críticos mapeados

1. **Convidado confirma presença**: recebe link → vê informações do evento → preenche RSVP → recebe confirmação visual e (futuramente) por e-mail.
2. **Convidado reserva presente**: acessa lista de presentes → filtra por categoria → se identifica (nome/telefone) → escolhe forma de presentear → recebe confirmação, item marcado como reservado para os demais.
3. **Casal acompanha status**: login → dashboard → visão de pendências → ação direta (reenviar lembrete, editar convidado) sem sair do contexto.

### 1.3 Estados a tratar explicitamente em toda tela de listagem

- Vazio (nenhum convidado/presente cadastrado ainda) — com call-to-action claro.
- Carregando.
- Erro de carregamento — com opção de retry.
- Populado — com paginação ou virtualização se a lista crescer muito (casamentos grandes podem ter 300+ convidados).

## 2. Interface do Usuário (UI)

- Interface do **site público** é fortemente visual e emocional (fotos do casal, tipografia expressiva), permitindo customização de tema por casamento — cor primária, cor secundária e par tipográfico (`--font-display`) aplicados globalmente via `layouts/default.vue` (`ui.store.ts` + `useWeddingTheme.ts`), cobrindo `/{slug}`, `/{slug}/presentes` e `/{slug}/rsvp` (com ou sem código) automaticamente, por herdarem do mesmo layout.
- Interface do **painel administrativo** prioriza escaneabilidade (tabelas, contadores, filtros), mas a linguagem visual do Design System é **100% compartilhada com o site público**: pill buttons com glow (`rounded="full"`, default da plataforma inteira), cartões com raio/sombra premium (`radius="xl"`/`elevation="xl"`, default de `UiCard`) e dropdowns modernizados aparecem também no admin (histórico de como se chegou a essa decisão — inclusive uma reversão de escopo — em `docs/CHANGELOG.md`). O painel herda a paleta de cores do casamento (`layouts/admin.vue`, mesmo mecanismo do site público). A fonte, porém, nunca varia: `--font-sans` é fixa em toda a plataforma, mesmo que o casal tenha escolhido um `fontPairId` diferente para o site público — legibilidade em densidade de dados prevalece sobre identidade visual aqui, e é a única exceção mantida por essa razão, não por preferência estética. **Único ponto de divergência visual mantido, deliberado**: o "lift" de hover dos botões pill (`hover:scale-[1.03]`) é suprimido no admin via `provide(ADMIN_UI_CONTEXT_KEY, true)` em `layouts/admin.vue`, injetado (default `false`) dentro de `UiButton` — glow, uppercase e o `active:scale` de clique continuam idênticos nos dois contextos; só o hover passivo muda (`app/utils/admin-ui-context.ts`, testado em `Button.spec.ts`).
- **Polimento "Admin Premium"**: cards, botões, campos de formulário e linhas de tabela têm transições suaves (`transition-brand`, ver seção 3.1). Cabeçalho de página padronizado via `AdminSection.vue` (`components/admin/`, slots `title`/`description`/`actions`); métricas do dashboard via `AdminStatCard.vue` (ícone + label + valor); atalhos de ação via `AdminQuickAction.vue`. Configurações organizada em abas (`UiTabs variant="segmented"`, ver seção 3.2) — Geral/Aparência/Conteúdo/Colaboradores — cada aba com **sub-menu de seções** próprio (ver a composição em 2.1). A variante `ghost` de `UiButton` tem uma borda sutil sempre visível (`border-border/60`), mesmo em repouso, pra não ler como texto solto sobre uma linha/card da mesma cor. Marca do sidebar/header do admin: "MeuSiteCasamento" (`layouts/admin.vue`), mesmo rebrand do público.
- Uso consistente de **estado vazio ilustrado** nas listagens administrativas para orientar o próximo passo do usuário.
- **Criar, editar e gerenciar um registro do admin acontece em modal**, não em tela dedicada — inclusive quando o conteúdo tem etapas ou várias frentes. O wizard de convidado roda dentro do modal (`UiModal` com `scroll="content"`, régua de passos e botões de navegação fixos, só o conteúdo do passo rolando). O convite abre um modal único que empilha tudo que era a tela de detalhe — dados do convite, convidados vinculados, link de acesso e Linha do Tempo — em blocos separados por divisor (`InviteSection`), nunca em cartões: dentro de um modal, cartão empilhado vira caixa dentro de caixa. Tela própria só quando o fluxo traz dado de fora (ex.: importação CSV com mapeamento de colunas).
- **Modal dentro de modal é proibido.** O que era um segundo diálogo (adicionar convidado ao convite, gerar link de acesso, confirmar exclusão de etiqueta) vira bloco inline dentro do mesmo modal, que expande no lugar — diálogo empilhado sobre diálogo prende o foco em duas camadas e some com o contexto que a pessoa estava lendo.
- **Modal de criar/editar é sempre governado pela URL** (`?novo=1` / `?editar=<id>`), nunca por um `ref` local: é o que mantém link salvo, resultado da busca global e botão Voltar do navegador funcionando — as rotas dedicadas que os modais substituíram (`/convidados/novo`, `/convidados/{id}`, `/convites/{id}`) viraram redirecionamento para essas queries.
- **Cabeçalho de lista do admin é uma barra de ferramentas.** No `#actions` de `AdminSection`, na ordem: filtro de texto (`UiInput` com `icon="lucide:search"`, `tone="muted"`, `w-full sm:w-64`, placeholder `"Filtrar por ..."`) e, à direita dele, o CTA primário. O CTA é sempre **verbo + entidade no singular** (`Adicionar convidado`, `Adicionar convite`, `Adicionar grupo`, `Adicionar presente`) com ícone `lucide:plus` — nunca `Novo <entidade>`, que é a forma reservada ao **título** do modal que o botão abre (`Novo convidado`). Dentro do painel (`AdminPanel #headerActions`) ficam só os controles que dependem da tabela logo abaixo: a barra de filtros ativos (`AdminTableFilterBar`) e, onde o recorte não é uma coluna (Ativos/Arquivados), os chips (`AdminFilterChips`). Em telas estreitas o cabeçalho empilha sozinho (`flex-col sm:flex-row`): filtro em largura cheia, CTA abaixo.
- **Recorte de lista é filtro da coluna, não fileira de chips.** Cada coluna da `AdminTable` declara o que sabe fazer (`filter: { type: 'text' | 'select' }` e/ou `sort: 'alpha' | 'numeric' | 'date'`) e ganha um menu no próprio cabeçalho, com duas seções fixas — "Ordenar" (par de rótulos conforme o tipo: `A a Z` / `Menor a maior` / `Mais antiga a mais recente`) e "Filtrar". **Coluna que não declara nenhum dos dois não abre menu nenhum**: filtro que não filtra de verdade — porque o endpoint não sabe aquele recorte, ou porque o valor é derivado só da página carregada — é pior que filtro ausente, já que quem usa confia nele. Uma coluna ordenada por vez. O chip (`AdminFilterChips`) fica reservado ao recorte que não é uma coluna (escopo da consulta, tipo Ativos/Arquivados).
- **Filtro de coluna com várias opções é caixa de marcar.** `filter.multiple` troca a lista de opções por `UiCheckbox` (input nativo — leitor de tela anuncia o estado e a barra de espaço funciona sozinha) e some com a linha "Todos": ali cada linha é uma marcação independente, e quem tira o recorte é o "Limpar" do topo do painel. Marcar duas opções vira dois chips na barra de filtros ativos, removíveis um a um — nunca um chip "2 selecionados", que obrigaria a reabrir o menu pra saber quais. Só declarar `multiple` quando o endpoint aceita lista naquele parâmetro; senão o segundo valor é ignorado em silêncio, que é o pior desfecho possível.
- **A lista não some enquanto o recorte é aplicado.** Esqueleto só na primeira carga: durante um refetch a tabela anterior continua na tela, esmaecida (`opacity-60` + `transition-brand`). Não é só polimento — trocar a tabela por esqueleto desmonta o cabeçalho e, com ele, o menu aberto onde o clique acabou de acontecer, tornando impossível marcar duas opções seguidas.
- **Filtro aplicado é sempre visível fora do menu.** `AdminTableFilterBar` desenha um `UiChip` removível por recorte ativo (`Faixa etária: Crianças`) mais "Limpar tudo" — sem isso o filtro fica escondido dentro do cabeçalho e a lista parece ter perdido registros. A mesma barra traz o botão "Filtros" que só existe abaixo de `md`, onde a tabela vira blocos empilhados e não há `<thead>` pra ancorar menu: ele abre um `UiModal` com o mesmo painel (`AdminColumnFilter`) de cada coluna, empilhado.
- **O estado dos filtros mora na URL**, um parâmetro por coluna com o nome da própria coluna, mais `?ordenar=&direcao=` (`useTableFilters`) — link de recorte compartilhável, botão Voltar desfazendo o filtro e não a página, e os links que já existiam (`/convidados?faixa=crianca`, do contador do dashboard) seguem valendo. Texto livre é escrito com atraso (`useDebouncedText`, 300ms): sem isso cada tecla vira uma navegação e uma requisição.
- **Lista longa se organiza em blocos recolhíveis, dentro da própria `AdminTable`** (prop `sections`, tipo `AdminTableSection`). Cada bloco é um cabeçalho que atravessa as colunas — rótulo, ícone e contagem — e é ele mesmo o controle de recolher (`<button>` de verdade: uma `<tr>` não é focável nem anunciada como controle). São no máximo **dois níveis** (grupo e subdivisão), e quem guarda o que está recolhido é a página (`collapsed-ids` + evento `toggle-section`), nunca a tabela: o estado precisa sobreviver a refetch. A contagem fica **ao lado do rótulo**, não empurrada à direita — numa tabela larga o `ml-auto` a jogaria para a borda da largura *rolável*, fora da área visível, porque o cabeçalho atravessa todas as colunas. Tabela sem `sections` desenha exatamente como antes: os dois modos passam pelo mesmo laço, e é isso que impede a marcação da linha de existir duas vezes no template.
- **Composição da tela em Modo Lista** (`/admin/{slug}/convidados/lista`): menu da seção à esquerda (`GuestSectionNav`), e no corpo, de cima para baixo — cabeçalho `AdminSection`, `AdminMetricStrip` com os números da lista inteira, linha compacta de contagem por categoria, o painel da tabela e, abaixo dela, o painel de rascunho. **Não** há coluna de resumo à direita: com a navegação do admin, o menu da seção e uma tabela de oito colunas na mesma tela, uma quarta coluna espremia a tabela até as colunas saírem de vista e o título quebrar uma palavra por linha. Os números descrevem sempre a lista inteira; o recorte é descrito pelo "N exibidas" do painel.
- **Coluna que o cabeçalho do bloco já responde não existe.** O Modo Lista não tem coluna "Grupo": o bloco diz de qual grupo a linha é, e repetir isso por linha custava uma coluna inteira — estreita, quebrava "Amigos do Trabalho" em três linhas e engordava a tabela toda. O recorte por grupo também não se perde: recolher tudo e abrir um bloco dá o mesmo resultado com menos cliques.
- **Toda tabela da plataforma é `AdminTable`.** Inclusive a listagem interna da equipe (`/plataforma`), que era o último uso da antiga `UiTable` — removida quando essa tela migrou. Uma tabela nova nunca reescreve `<table>`/`<th>` à mão: declara colunas e usa `AdminTable`, senão perde cabeçalho fixo, filtro por coluna e o formato empilhado do celular de uma vez só.
- **Filtro de lista carregada inteira é aplicado por `applyTableFilters`** (`app/utils/table-rows.ts`), com a página declarando por coluna de onde sai o valor e como se compara — quase toda coluna interessante é derivada (o status de um presente vem das reservas, a categoria vem de outra requisição). Vale **só** para tela que carrega tudo de uma vez: em lista paginada o recorte é sempre do endpoint, senão filtra só a página carregada e o resultado descreve uma lista diferente da que o cabeçalho e a paginação anunciam.
- **Quem rola numa lista do admin é a grade, não a página.** A área da tabela é o contêiner de rolagem (`.table-scroll`, `app/assets/css/main.css`: `overflow: auto` em qualquer largura, `max-height: 60vh` do `md` pra cima) e o `<thead>` é `sticky top-0` dentro dela. Assim o cabeçalho de colunas, a barra de filtros ativos e a paginação ficam em vista ao mesmo tempo, sem `position: fixed` em nada. Como a caixa rola nos dois eixos, tabela larga volta a rolar na horizontal junto — o que não caberia se o cabeçalho dependesse do scroller da página. Fundo do cabeçalho é **opaco** (`bg-surface-muted`, nunca `/50`): parado sobre as linhas, o meio-tom deixa o conteúdo passar por baixo; borda e fundo ficam na `<th>`, porque é a célula que gruda. `AdminTable :scrollable="false"` devolve a rolagem para a página em lista curta por natureza (prévia do dashboard).
- **`overflow-hidden` em volta de tabela é proibido — use `overflow-clip`.** Os dois recortam igual, mas `hidden` transforma o elemento em contêiner de rolagem, e a partir daí todo `sticky` de dentro se ancora nele em vez do scroller de verdade: o cabeçalho fixo continua declarado e simplesmente não acontece. É a mesma armadilha que a nota de `overflow-x` em `app/assets/css/main.css` documenta (`overflow-x: hidden` computa `overflow-y: auto`), e por isso `AdminPanel` usa `overflow-clip`.
- **Seletor de recorte tem dois pesos** (`AdminFilterChips`). `variant="chip"` (default) é o recorte discreto do cabeçalho de painel: some no fundo até ser procurado, que é o certo para um filtro secundário sobre uma lista que já está na tela. `variant="segmented"` é a mesma escolha promovida a controle principal, na moldura de largura cheia de `UiTabs variant="segmented"` (ativo preenchido com a primária) — para quando escolher o recorte **é** a tarefa, e não um ajuste: é o caso dos modelos prontos do gerador de planilha de importação. O componente continua um só porque a semântica é a mesma (escolha mutuamente exclusiva); o que muda é a hierarquia visual. Não confundir com `UiTabs`: aquilo troca o painel exibido, isto muda o conteúdo de um painel que continua o mesmo.
- **Num quadro de altura limitada, o conteúdo principal é quem fica com a altura.** No gerador de modelo, o seletor de recorte, o cabeçalho e o ajuste da linha de exemplo somavam mais altura que a própria lista de colunas — o quadro mostrava um item por vez. A correção é a forma geral: cromo fixo comprimido a uma linha cada (descrição do recorte na mesma linha do cabeçalho, ajuste secundário reduzido a um `UiCheckbox` com o aviso ao lado em vez de `ToggleRow` mais caixa de apoio) e a área rolável com `flex-1` **e** um `min-h` — sem o piso, o `flex-1` cede tudo ao cromo numa janela baixa.
- **Item selecionado numa lista de escolha se destaca por borda** (`border-primary/50`), como o item aberto de acordeão (ver 2.1) — nunca por um terceiro tom de preenchimento, que deixa a pilha turva sem dizer o que está marcado.
- **Item obrigatório de uma lista não é um controle desativado.** Uma caixa marcada e esmaecida (`disabled` + `opacity-50`) lê como "indisponível", não como "sempre vem junto" — o gerador de modelo troca o `UiCheckbox` por ícone de cadeado mais `UiBadge tone="primary"` no item que não se pode desmarcar. A regra vale para qualquer lista com item fixo.
- **Linha de tabela do admin abre o registro.** `AdminTable` tem `row-clickable` + evento `row-click`; o nome na primeira coluna é um `<button>` (o alvo acessível de verdade, já que uma `<tr>` não é focável) e a linha inteira é conveniência de mouse. Clique num controle da linha (excluir, expandir) é dele, não da linha — `AdminTable` filtra por `closest()` antes de emitir.
- Toasts para feedback de ações assíncronas (sucesso/erro), nunca `alert()` nativo do navegador.
- **Hero com contagem regressiva e atalhos embutidos, personalizáveis**: a contagem regressiva vive dentro do próprio `Hero.vue`, nas duas variantes (com/sem foto de capa), condicionada a `config_tema.showCountdown`. Logo abaixo, uma linha de `UiButton` em formato pill (`rounded="full"`) com os atalhos que o casal escolher — catálogo fixo de 8 possíveis em `shared/hero-buttons.ts` (`HERO_BUTTON_CATALOG`: Presentes, Confirmar presença, Cerimônia e festa, Manual do convidado, Dress code, Nossa história, Galeria, FAQ), seleção e destaque editáveis em `/admin/configuracoes` (Aparência → "Atalhos do Hero"). Persistido em `config_tema.heroButtons` (array de ids) e `config_tema.heroFeaturedButton` (o único que recebe `variant="primary"`; os demais ficam `variant="outline"`). `resolveHeroButtons()` ignora ids desconhecidos/removidos do catálogo silenciosamente — nunca quebra o Hero por causa de uma seleção antiga. `casamento.nomes_noivos` no formato `"Nome & Nome"` é dividido em 3 linhas (`Nome` / `&` / `Nome`) para o tratamento tipográfico grande do Hero — nomes fora desse padrão caem no fallback de uma linha só. A linha da data mostra o nome do local (`nome_local` da primeira `etapa_evento` cadastrada, normalmente a Cerimônia).
- **`shared/home-sections.ts` é a fonte única das seções da home.** Dele saem três coisas que precisam concordar: a ordem dos capítulos, quais aparecem e o catálogo de atalhos do Hero. Mantê-las em listas separadas foi o que produziu, na primeira rodada desta fase, um catálogo de oito atalhos para onze seções — três seções inalcançáveis pelo Hero, sem nada acusando a falta. `shared/hero-buttons.ts` passou a ser derivado (`HOME_SECTION_CATALOG.map(...)`), com `LEGACY_HERO_BUTTON_IDS` traduzindo os dois ids que mudaram (`cronograma`→`grande-dia`, `galeria`→`nossos-momentos`) — sem esse mapa, quem tivesse esses atalhos salvos os perderia em silêncio, porque id desconhecido é (corretamente) descartado.
- **A ordem dos capítulos é do casal, não do código.** `app/pages/[slug]/index.vue` percorre `config_tema.sectionOrder`, editável em Aparência → "Ordem das seções". O **Hero fica fora do catálogo**: é a capa, não um capítulo. `resolveHomeSectionOrder()` dá três garantias que a lista salva sozinha não dá — descarta id que saiu do catálogo, descarta repetição, e **anexa no fim as seções ausentes da lista salva**. Essa última é a que importa: sem ela, toda seção lançada depois de o casal salvar a ordem nasceria invisível para quem já personalizou. `order` define a **sequência**, nunca o conjunto — para restringir o conjunto existe `hiddenSections`. A página usa `v-for` sobre ids com um `v-if` por seção, não `<component :is>`: cada seção recebe props diferentes, e um mapa id→componente precisaria de um mapa id→props ao lado. A ordem **padrão** é a do protótipo: Versículo como respiro depois das boas-vindas, RSVP logo após "O Grande Dia" (a pergunta chega enquanto o convidado acabou de ler onde e quando), Galeria fechando antes do FAQ.
- **O fundo de cada seção vem da POSIÇÃO, não do componente.** `resolveHomeSections()` percorre as seções que de fato aparecem e reveza `default`/`muted` entre elas. Enquanto o tom era fixo dentro de cada componente (`tone="muted"` no Grande Dia, no Manual, no FAQ), bastava o casal reordenar a página — ou uma seção sumir por falta de conteúdo — para duas seções claras iguais ficarem coladas. Versículo (`primary`) e RSVP (`accent`) têm tom próprio e ficam fora do revezamento **sem interrompê-lo**: o que a alternância evita é duas claras iguais adjacentes, e uma faixa escura entre elas não muda esse risco. É por isso que a página precisa saber a visibilidade **antes** de renderizar: `app/utils/home-section-content.ts` monta esse mapa com os mesmos predicados (`hasVerseContent` e companhia) que os componentes usam nos próprios `v-if`, então as duas decisões não têm como divergir.
- **Ocultar seção é um interruptor próprio** (`config_tema.hiddenSections`), ao lado do conteúdo vazio — não no lugar dele. As duas coisas respondem a perguntas diferentes: "não tenho o que dizer aqui" e "tenho, e não quero mostrar agora". Sem o interruptor, tirar o dress code da página exigia apagar um texto que o casal talvez queira de volta na semana seguinte. Seção desligada some do site, dos atalhos do Hero e do menu (um atalho para seção desligada é um link que não faz nada), mas **continua na lista do admin**, na posição dela, esmaecida — tirá-la de lá faria o casal perder de vista que ela existe e em que ordem voltaria.
- **Versículo** (`PublicVerseSection`): faixa cheia na cor primária com o texto em `--color-ornament`, o "respiro" que no convite impresso separa duas páginas claras. Deliberadamente **não** usa `PublicEditorialSection` — não tem eyebrow, título nem divisor, porque é uma pausa na leitura e o tratamento de "capítulo" a transformaria em mais uma seção com cabeçalho. É a única seção do site **sem texto padrão de plataforma**: um versículo genérico apareceria no site de todo mundo falando em nome da fé de um casal que nunca escolheu aquilo. Sem texto, não existe.
- **Manual dos Padrinhos** (`PublicGroomsmenManualSection`): traje "para eles"/"para elas" e a paleta em bolinhas (`PublicColorSwatches`), separado do Manual dos Convidados por ter outro público e outra informação. Cada bloco aparece só se preenchido, e um cartão de traje sozinho ocupa a linha inteira — ao lado de um vazio, pareceria conteúdo faltando em vez de escolha.
- **Monograma** (`PublicMonogram`): a assinatura recorrente do convite — capa, navegação e rodapé. Dois caminhos, nessa ordem: a arte enviada pelo casal (`config_tema.monogramImageUrl`) ou as iniciais derivadas de `"Nome1 & Nome2"` com um coração em ornamento. O fallback é o caso **normal**, não um consolo: a maioria dos casais não tem monograma desenhado. Nome fora do padrão de dois nomes não rende iniciais confiáveis, e aí o componente não desenha nada — carimbar a letra errada no site inteiro é pior que não ter monograma. É sempre `aria-hidden`: o nome do casal já está escrito ao lado em toda posição em que ele aparece.
- **A página não tem elevação, e a transição entre capítulos é seca.** Duas decisões do protótipo que valem juntas: cartão é retângulo de traço fino sobre papel (`border-border/70`, sem `shadow`), e a passagem de uma seção para a outra é uma troca de fundo, sem a costura curva que existia antes. A única onda do site é a que fecha o Hero. A curva repetida a cada seção dava à página um movimento de landing page que briga com o registro de papel impresso — e, com a moldura de filete ligada, as duas se cortavam.
- **O divisor de seção é um filete único** (`UiSectionDivider`), não o losango entre pontos de antes: o protótipo não tem ornamento composto em lugar nenhum, e o losango repetido dez vezes ao descer a home virava um enfeite insistente.
- **`UiAccordion variant="rule"`** é o das dúvidas frequentes: sem moldura, só uma régua entre as perguntas. Uma pilha de dez cartões com sombra virava uma parede de caixas justamente onde a página deveria ficar mais leve. A variante `card` segue disponível; `plain` continua sendo a das listas de configuração do admin.
- **As sugestões de dress code são cartões**, no mesmo molde dos marcos de "Nossa História" e dos tópicos do Manual: borda fina, alinhado à esquerda, em grade. As cápsulas que havia antes só funcionavam para rótulos de duas ou três palavras; com uma frase inteira dentro, cada uma virava uma barra de largura diferente da vizinha. Os três cartões do site são o mesmo cartão — `border-border/70`, sem sombra, ícone em disco dourado.
- **Nenhuma arte é desenhada pela plataforma.** A ilustração de traje que existia no Dress Code e no Manual dos Padrinhos foi removida: era a única imagem do site que não vinha do casal, aparecia igual em todo casamento e destoava de uma página feita de tipografia e filete. No lugar dela, `config_tema.dressCodeImageUrl` — uma referência de traje **opcional** enviada pelo casal. Sem ela, a seção é texto e cartões, que é o que o protótipo do convite faz: a imagem é um extra, não um buraco a preencher.
- **Nossa História tem duas formas, e o casal escolhe pelo conteúdo**: texto corrido (com foto opcional ao lado) ou a fileira de cartões de marcos (`config_conteudo.storyMilestones`, rótulo + título + texto). Texto corrido é uma carta; marcos são uma linha do tempo. Havendo marcos, eles ganham — quem escreveu três marcos escolheu contar assim.
- **`PublicPageHeader`** dá às páginas públicas que não são a home (presentes, RSVP, galeria) o mesmo tratamento de capítulo das seções: eyebrow miúdo em caixa alta, título em display e o filete de ornamento. Não reusa `PublicEditorialSection` porque ali o título é um `<h2>` — uma seção dentro da página; aqui é o `<h1>`, o assunto da página inteira. Antes cada página montava o próprio cabeçalho com corpo e espaçamento diferentes, e as três pareciam de sites diferentes ao lado da home.
- **A margem lateral do site público é 24px (`px-6`), em toda página e em toda faixa** — cabeçalho, Hero, seções e páginas internas. O cabeçalho ficou com 16px por um tempo e era o ponto mais apertado da página, justamente onde o olho bate primeiro.
- **`UiCard elevation="none"`** é o padrão do site público desde o rebrand: cartão é traço fino sobre papel. O degrau existe no componente (e não só como classe solta nos chamadores) para que cartão de domínio — o de presente, por exemplo — continue usando `UiCard` em vez de reconstruir a moldura à mão.
- **`PublicMobileCtaBar`**: barra fixa no rodapé do celular com "Confirmar presença", e um espaçador da mesma altura logo abaixo (sem ele as últimas linhas da página ficam sob a barra, sem poderem ser roladas até aparecer). Só no celular — no desktop o cabeçalho fica visível o tempo todo com o mesmo destino. Some quando o casal desliga a seção de RSVP: insistir num CTA que ele desligou contraria a escolha dele. O botão "voltar ao topo" sobe para `bottom-24` no celular para não pousar em cima dela.
- **Estilo de título e moldura são opções de tema, não de componente.** `config_tema.headingStyle: 'engraved'` escreve nomes e títulos em caixa alta com espaçamento largo, via `[data-heading-style]` no wrapper do layout público — nunca em `<html>`, senão o painel administrativo herdaria a caixa alta do tema de um casamento. O `h1` do Hero desce um degrau de tamanho quando 'engraved' está ativo, porque caixa alta come largura e a compensação tem de morar junto das classes de tamanho, não no CSS global que não conhece o corpo de cada título. `config_tema.ornamentFrame` desenha a moldura de filete duplo (`.ornament-frame`, pseudo-elementos, só a partir de `sm` — num celular a régua de 24px de cada lado comeria a largura útil de leitura em troca de um enfeite). O raio é o mesmo `--radius-lg` dos cartões que vivem dentro dela: com cantos vivos, a moldura passava por fora de cartões arredondados sem nenhuma relação de forma entre as bordas, e lia como dois desenhos sobrepostos em vez de uma peça dentro da outra. **Moldura ligada desliga a costura curva das seções** — as duas são metáforas opostas (a curva diz "as seções escorrem uma na outra"; a moldura diz "cada seção é uma página do convite") e, juntas, a onda passava por cima da borda enquanto o retângulo cortava a onda no meio. Com moldura, é a própria moldura que separa uma seção da outra. A moldura chega às seções por **injeção** (`PUBLIC_ORNAMENT_FRAME_KEY`), não por prop nem pelo store: como prop, cada uma das dez seções teria de repassá-la e esquecer uma deixaria a página com moldura pela metade; pelo store, as ~20 suítes de teste de seção passariam a exigir Pinia só para decidir uma borda. `inject` tem valor padrão, e é isso que mantém o contrato de "testável com `@vue/test-utils` puro" de `components/public/`.

### 2.1 Composição da tela de Configurações

Tela longa de formulário não é uma pilha de campos: é um índice. O padrão (refatorado a partir do modelo "Config Harmony") tem quatro camadas, e vale para qualquer tela futura de configuração:

1. **Abas** (`UiTabs variant="segmented"`) — o assunto: Geral, Aparência, Conteúdo, Colaboradores.
2. **Sub-menu de seções** (`AdminSettingsTabLayout`) — coluna de 12rem fixa na rolagem (`lg:sticky lg:top-20`, os 4rem do header mais folga) com o rótulo da aba, uma linha dizendo do que ela trata e âncoras nativas (`<a href="#id">`) para as seções. Abaixo de `lg` vira faixa de chips rolável acima do conteúdo; aba de uma seção só não ganha faixa.
3. **Cartão de seção** (`AdminSettingsSectionCard`) — a unidade que a âncora endereça (`sectionId` + `scroll-mt-24`), com título, subtítulo e campos no mesmo bloco de padding. Slot `#aside` opcional desenha a caixa "Como funciona" (`bg-surface-muted/60`) numa coluna de 1/3 a partir de `xl`, e abaixo disso vira o último bloco da coluna única — é onde vai a explicação longa de regra de negócio (ex.: o que a InfiniteTag libera), em vez de um parágrafo de seis linhas colado sob o campo.
4. **Barra de salvamento** (`AdminSettingsSaveBar`) — `sticky bottom-4` **dentro** do `<form>` de cada aba, com o rótulo de ação específico ("Salvar dados do evento") e o `meta.dirty` do VeeValidate daquele formulário; "Descartar" volta aos valores do servidor pelo mesmo `resetForm` que popula o formulário. **Só existe quando há alteração pendente** (entra/sai com transição): sem nada para salvar, uma barra permanente ocupa a base da tela sem oferecer ação nenhuma. Uma barra por aba, não uma na página: uma barra única teria de adivinhar qual formulário submeter.

Duas regras de conteúdo que a tela firmou:

- **Preset e ajuste manual não convivem no mesmo nível.** A aba de Aparência separa "Opções de tema" (a grade de presets, que já define tipografia e as duas cores de uma vez) de "Opções avançadas" (tipografia, cor primária, secundária e cores de texto, atrás de uma revelação). Tê-los lado a lado dava a impressão de configurar a mesma coisa duas vezes. As opções avançadas nascem abertas quando o tema já está personalizado — quem salvou cores próprias precisa ver onde estão os valores em vigor, e não uma grade de presets sem nenhum marcado.
- **Item aberto se destaca por borda, não por preenchimento.** O cartão de seção é branco (`surface-elevated`) e os campos dentro dele são off-white (`surface`); um terceiro tom quente no item aberto de um acordeão deixava a pilha turva sem dizer qual item estava aberto. Ver a variante `plain` de `Accordion` em 3.2.

Primitivas de campo da tela, todas em `components/admin/settings/`:

- `Field` — rótulo + apoio para controles **compostos** (`role="group"` + `aria-labelledby`, porque um `<label>` só nomeia um controle). Campo simples usa o `label`/`hint` do próprio `UiInput`.
- `ToggleRow` — linha de recurso ligado/desligado, com a linha inteira clicável. Construída sobre `SwitchRoot` do Reka, **não** sobre um `<input type="checkbox">` em `sr-only` (ver a lição de foco escondido em 3.1). O `SwitchRoot` é a própria linha, e não só a chavinha, para que clicar no texto também alterne — com `<label for>` não funcionaria, porque o primitive renderiza um `<button>`. `UiCheckbox` continua sendo para *selecionar itens de uma lista*.
- `ColorPicker` — um campo de cor da paleta: o `UiColorPicker` mais o `ContrastHint` abaixo. Suprime o erro de campo quando a cor é um hexadecimal válido reprovado no contraste: o aviso já explica e resolve o caso, e mostrar os dois repetia a mesma frase empilhada. O erro de *formato*, que o aviso não cobre, continua aparecendo.
- `ContrastHint` — leitura de contraste em linguagem de gente. Diz a consequência ("deixa o texto difícil de ler") em vez da razão e do limiar, e oferece a correção em um clique via `suggestAccessibleColor()` (ver 3.4).
- `UploadBox` — a moldura das fotos de Branding: borda tracejada com rótulo e orientação de formato quando vazia; prévia estática com as ações Editar/Trocar/Remover quando preenchida. O tracejado comunica "espaço a preencher" e por isso desaparece com a foto lá. A prévia é **estática** de propósito — ver `ImageEditorModal`.
- `ImageEditorModal` — cortar, girar e aproximar a foto (`vue-advanced-cropper`), com proporção travada por uso (16:9 na capa, 4:5 na história, as mesmas que o site desenha; corte livre só empurraria o problema de enquadramento para o site público). Substituiu o seletor de ponto de foco que ficava aberto no cartão e **persistia a cada arraste** — cada micro-ajuste virava requisição e recarregamento, e a tela piscava durante o enquadramento. Aqui nada sai do editor até "Aplicar": o recorte vira JPEG e sobe numa chamada só, e o ponto de foco volta ao centro porque o corte passa a ser a única fonte de enquadramento. `canvas.toBlob` lança em imagem de outra origem sem cabeçalho CORS — o caso é tratado com mensagem, não com erro cru.
- `HeroShortcutsField` — switch mestre dos atalhos do Hero mais um switch por atalho do catálogo e o seletor de destaque. O "desligado" não precisou de campo novo em `config_tema`: lista vazia de `heroButtons` já significa "nenhum botão" para `resolveHeroButtons`, que só cai no padrão quando o valor é nulo.
- `OrnamentField` — o campo da cor de ornamento. Não reusa o `ColorPicker` acima porque a régua é outra: aqui não há reprovação por contraste contra o fundo claro (ver 3.4). O aviso que sobra é condicional — só aparece quando a seção Versículo tem texto, porque é a única situação em que o ornamento vira letra, e mede contra a **cor primária em edição no formulário**, não contra a salva. É aviso, nunca bloqueio.
- `SectionOrderField` — a ordem dos capítulos da home. Arrastar (drag nativo do HTML5, sem biblioteca: onze itens fixos não pedem virtualização nem sensores de toque) é a interação principal, mas **não é a única** — cada linha tem "subir" e "descer" em `<button>` de verdade, com `aria-label` nomeando a seção. Reordenar é a função inteira da tela; se dependesse do mouse, ela simplesmente não existiria para quem usa teclado ou leitor de tela. Não há interruptor de "ocultar": esvaziar o conteúdo já esconde a seção, e um segundo estado dizendo a mesma coisa sairia de sincronia.

`AdminCoverImageUploader`/`AdminStoryImageUploader`/`AdminMonogramUploader` são configuração fina em cima de `UploadBox` (+ `ImageEditorModal` nos dois primeiros): antes eram dois arquivos ~95% idênticos, e toda a diferença real entre eles é rótulo, proporção e qual par upload/remove usar. O `UploadBox` ganhou `previewFit` e `editable` para o monograma: `object-contain` sobre `aspect-square` porque uma marca transparente precisa ser vista inteira (recortá-la mostraria uma prévia que não corresponde ao site), e sem a ação de corte, que ali não teria decisão nenhuma a oferecer — com isso "Trocar" vira a ação primária da caixa, senão sobrariam um ghost e um destrutivo sem nada entre eles. `ThemePresetPicker`/`FontPairPicker` deixaram de desenhar rótulo próprio — quem rotula é o `Field` do chamador, para a tela não ter dois padrões de rotulagem concorrentes.

## 3. Design System

### 3.1 Fundamentos (tokens)

- **Cor**: paleta base neutra (escala de cinzas) + duas cores de "tema do casamento" configuráveis, `primary` e `secondary` (aplicadas via CSS variables, permitindo customização por evento sem alterar código — ver seção 3.3).
  ```css
  :root {
    --color-primary: #6b4a35; /* customizável por casamento */
    --color-primary-foreground: #ffffff;
    --color-secondary: #5f6f52; /* customizável por casamento */
    --color-secondary-foreground: #ffffff;
    --color-surface: #fbf9f5; /* off-white — fundo de página, fixo na plataforma */
    --color-surface-elevated: #ffffff; /* branco puro — cartões/conteúdo em destaque (Card.vue), flutuando sobre o off-white */
    --color-surface-muted: #f2ece2;
    --color-border: #e8ddd0;
    --color-text: #2b2622;
    --color-text-muted: #6b6259;
    --color-heading: var(--color-text); /* customizável por casamento (titleColor, opcional — modo de cor avançada) */
    --color-body: var(--color-text); /* customizável por casamento (bodyColor, opcional — modo de cor avançada) */
    --color-ornament: var(--color-secondary); /* customizável por casamento (ornamentColor, opcional — filetes/divisores/monograma) */
  }
  ```
  > Os neutros acima foram calibrados contra o protótipo do rebrand: marfim um fio mais claro e mais quente, bege de seção alternada com menos distância do marfim (o contraste entre as duas faixas era forte demais para uma página que deve parecer papel), cartão que não é branco puro. Continuam fixos da plataforma inteira.
  > `#6b4a35` (mesma família de tom do preset "Clássico Elegante") é o default real (`app/assets/css/main.css`, `shared/utils/contrast.ts#DEFAULT_PRIMARY_COLOR`), validado ≥4.5:1 de contraste contra `--color-surface` — `#a8785c` fica citado em `tests/unit/utils/contrast.spec.ts` só como caso de rejeição conhecido (~3.81:1, achado real documentado em `docs/CHANGELOG.md`).
  > **Off-white vs. branco puro**: `--color-surface`/`--color-surface-muted`/`--color-border` são tons neutros fixos da **plataforma inteira** (público e admin) — não variam por casamento, ao contrário de `primary`/`secondary`/`heading`/`body`. `--color-surface-elevated` existe à parte para dar profundidade sutil (cartão branco puro sobre página off-white, como um editorial impresso); `shared/utils/contrast.ts` continua validando contra branco puro como pior caso.
  > **`--color-ornament` (Fase Rebrand do Convite)**: a cor do que é puramente decorativo — filetes, divisores (`UiSectionDivider`), o "&" e o ramo do Hero, a moldura de seção, o coração do monograma. Existe porque o dourado de um convite impresso (#C8A56A ≈ 2,3:1 contra o marfim) é a cor certa para um filete e a errada para texto, e a validação de contraste, corretamente, não distingue as duas coisas — ver seção 3.4. **Default = `var(--color-secondary)`**, não um dourado fixo: quem nunca escolheu um ornamento vê exatamente o site que já via, e um dourado imposto brigaria com a paleta de um casal que escolheu verde. Nenhum componente usa este token em texto de corpo, título ou rótulo de botão; a única aparição como texto é a seção Versículo, sobre a faixa na cor primária.
- **Três famílias no site público, com papéis distintos**: `--font-display` (títulos, varia por par tipográfico), `--font-sans` (corpo e interface, fixa) e `--font-serif` (**Cormorant Garamond, fixa**) — a serifada de *citação*, usada só onde o texto é fala e não informação: as boas-vindas, em itálico grande. É o que separa a voz do casal do resto da página. Separada de `--font-display` de propósito: a de display pode ser uma capitular como a Cinzel, que em texto corrido itálico seria ilegível. Os pesos e o estilo itálico são declarados em `nuxt.config.ts` — sem isso o módulo baixa só o romano e o navegador inclina a fonte por conta própria, um itálico falso visivelmente torto num corpo grande.
- **Tipografia**: um par tipográfico por casamento — uma fonte serifada de destaque (`--font-display`, aplicada ao site público) e uma fonte sans-serif fixa de plataforma (`--font-sans`, aplicada a corpo de texto e a todo o painel administrativo, nunca customizada por casamento — legibilidade em densidade de dados). O casal escolhe o par via `shared/theme-presets.ts#FONT_PAIRS`, independentemente da paleta de cores (ver seção 3.3). Cada par pode opcionalmente definir uma terceira família só para botões/CTAs (`FontPair.buttonFontFamily`, ex.: preset `vermelho-classico` usa Montserrat) — resolvida para `--font-button` por `useWeddingTheme.ts` (mesmo padrão condicional de `--color-heading`/`--color-body`: só entra quando o par a define) e aplicada globalmente por `UiButton` (`[font-family:var(--font-button)]` nas classes base). Sem sobrescrita, `--font-button` cai em `var(--font-sans)` (default declarado em `main.css`) — pares sem `buttonFontFamily` não mudam de aparência. Como só o layout público injeta `--font-button` (`includeFont: true`; o admin usa `includeFont: false`, mesma regra de `--font-display`), botões do painel administrativo nunca variam por casamento.
- **Espaçamento**: escala baseada em múltiplos de 4px (Tailwind spacing scale padrão, sem customização salvo necessidade real).
- **Raio de borda e sombra**: escala limitada (`--radius-sm/md/lg/xl`, `--shadow-sm/md/lg/xl`) fixa na plataforma — não varia por tema, aplicada consistentemente via os componentes de `components/ui/`; nenhum valor arbitrário de `border-radius`/`box-shadow` direto em componentes de domínio. O tier `xl` é o **default de `UiCard`** na plataforma inteira, público e admin.
- **Rolagem e `position: sticky`**: `overflow-x: hidden` fica **só no `html`**, nunca no `body`. `overflow-x: hidden` computa `overflow-y` para `auto`, então declarar isso no `body` o transforma num contêiner de rolagem próprio — e todo `sticky` descendente passa a se ancorar no scrollport do body, que tem a altura do conteúdo e nunca rola, ficando parado na posição estática. Foi o que manteve o header do admin, o header do site público e a barra de salvar de Configurações sem acompanhar a rolagem por um tempo: os três estavam escritos corretamente, o contêiner é que anulava o efeito. Regra prática: antes de "consertar" um `sticky` que não cola, procurar `overflow` em algum ancestral.
- **A chrome do admin não depende de `sticky` no documento — de propósito.** Corrigir o `overflow` do `body` não bastou: ao abrir qualquer Select ou Popover, o Reka aplica **trava de scroll no `body`** (`overflow`, `paddingRight`, `marginRight`) e recria a mesma condição em runtime. Com a página rolada, a sidebar saltava de volta ao topo do documento e saía de vista. Por isso `layouts/admin.vue` é um **app shell de altura fixa**: a raiz é `h-screen overflow-hidden`, o `<main>` é o único scroller (`overflow-y-auto`) e sidebar/header ficam fora dele. O documento também é travado (`bodyAttrs`), porque dois eixos de rolagem competindo fazem o navegador mover o de fora ao revelar um elemento focado. Regra: em app shell, `sticky` de chrome ancora no scroller interno, nunca no documento — e o offset é a folga de respiro, não a altura do header (que já está fora do scroller).
- **Nada focável escondido atrás de `sr-only`.** Um `<input>` de 1px em `position: absolute` continua sendo alvo de foco, e o navegador rola até ele: clicar num switch levava `document.scrollTop` de 111 para 708 sem o `<main>` mexer um pixel. Controle booleano/de escolha usa os primitives do Reka (`SwitchRoot`, `RadioGroupRoot`), que são `<button role="switch">`/`role="radio"` visíveis e já em vista. `sr-only` segue válido para *texto* auxiliar de leitor de tela — nunca para o controle em si.
- **Movimento**: `--transition-duration` (200ms) e `--transition-easing` (`cubic-bezier(0.16, 1, 0.3, 1)`), consumidos pela utility `.transition-brand` (`app/assets/css/main.css`, `@layer utilities`) — duração/easing únicos para toda a plataforma, para que ajustar a "sensação" das transições seja uma mudança de 2 variáveis, não de dezenas de componentes. Usada em todos os componentes de `components/ui/` (Button, Card, Input, Select, Textarea, Checkbox, RadioGroup, Toast) e em hovers/transições de página do admin (nav ativa, linhas de tabela). O glow colorido dos CTAs pill primary também é uma utility própria (`.shadow-glow-primary`), reaproveitada por qualquer botão flutuante fora do `UiButton` (ex.: `ScrollToTopButton.vue`) — nunca reescrever o `color-mix()` como valor arbitrário.

### 3.2 Componentes base (`components/ui/`)

| Componente | Responsabilidade |
|---|---|
| `Button` | Variantes: `primary`, `secondary`, `outline` (borda sutil `border-primary/25` + fundo translúcido `bg-surface-elevated/70` com `backdrop-blur-sm` — CTAs secundários sobre fundo claro ou foto), `ghost` (borda sutil `border-border/60` sempre visível, mesmo em repouso, pra não ler como texto solto sobre um card/linha da mesma cor), `destructive`; tamanhos `sm/md/lg`; prop `rounded` (`'full'` = pill, **default da plataforma inteira**; `'md'` só onde pedido explicitamente). CTAs em pill ganham rótulo uppercase tracked e, quando também `variant="primary"`, um glow colorido (`.shadow-glow-primary`) em toda a plataforma. O "lift" de hover (`hover:scale-[1.03]`) é a única parte condicional: presente no site público, suprimido no admin via `inject(ADMIN_UI_CONTEXT_KEY)` (provido por `layouts/admin.vue`) — ruído visual numa tela com dezenas de botões pill lado a lado; `active:scale-95`/glow/uppercase continuam iguais nos dois contextos. Prop `to` (renderiza como `NuxtLink` em vez de `<button>`, mesmas classes de variante) + `target` (só com `to`, ex.: `"_blank"`, aplica `rel="noopener noreferrer"` automaticamente) |
| `Input` / `Textarea` / `Select` / `Checkbox` / `RadioGroup` | Campos de formulário com estado de erro integrado; foco/hover com `.transition-brand`. **`Select` não é mais um `<select>` nativo** — é headless via Reka UI (`SelectRoot`/`Trigger`/`Portal`/`Content`/`Item`), com a lista desenhada por nós: raio, sombra suave, item ativo destacado (`bg-primary/[0.06]` + peso) e marca de check. A lista do sistema operacional não aceitava nenhum desses tratamentos e lia como um controle de 2010 ao lado do resto do Design System; o primitive entrega o que não se reimplementa sem bug (setas/Home/End, busca por digitação, `aria-activedescendant`, devolução de foco). Vai num `SelectPortal` com `z-60` porque muitos formulários do admin abrem em `UiModal` (`z-50`). **`RadioGroup`** desenha as opções como cartões com estado selecionado real (borda + `bg-primary/[0.04]`), props `layout` (`'stack'` default / `'grid'`), `icon` e `description` por opção — é a alternativa ao dropdown quando há 2 a 4 opções e cada uma precisa de uma linha de explicação (ex.: lista de convidados aberta vs. fechada). `Input`/`Textarea`/`Select` têm prop `hint` — linha de apoio abaixo do campo (o *porquê* da regra de negócio; o formato esperado continua sendo `placeholder`), somada ao erro no `aria-describedby`, para o leitor de tela ler a orientação **antes** de o usuário digitar. Substituiu os parágrafos `-mt-2 text-xs text-text-muted` que eram escritos à mão ao lado de cada campo. `Input` tem também `ariaLabel`, para quando o rótulo visível pertence a um grupo maior (par swatch + hexadecimal do seletor de cor) |
| `CurrencyInput` | Campo de dinheiro em BRL: `v-model` é **centavos** (`number | undefined`), nunca texto — prefixo fixo "R$" dentro do campo e máscara de acumulação (só dígitos, entram pela direita: `1` → `0,01`, `100` → `1,00`), o padrão brasileiro. Único componente de entrada de valor da plataforma (preço/valor-alvo/valor de cota no admin, contribuição livre no site público) — nunca reintroduzir um `UiInput` de texto convertendo reais a cada tecla, que empurrava os dígitos para a direita da vírgula (ver `docs/CHANGELOG.md`) |
| `DatePicker` | Campo de data headless (`PopoverRoot` + `CalendarRoot` do Reka). Substitui o `<input type="date">`, que só abria o calendário quando se acertava o ícone e desenhava o calendário do navegador (aparência diferente por sistema operacional, fora da paleta do tema). Aqui o **campo inteiro** é o gatilho e a navegação tem dois passos — mês a mês (‹ ›) e ano a ano (« ») —, porque data de casamento costuma estar a anos de distância. `v-model` é sempre a string `YYYY-MM-DD`: o `CalendarDate` do `@internationalized/date` não escapa do componente, então nenhum schema Zod ou coluna mudou de tipo. Prop `clearable` para campos opcionais |
| `TimePicker` | Campo de horário segmentado (`TimeFieldRoot` do Reka): hora e minuto separados, cada um aceitando digitação direta e setas ↑/↓, mais um atalho para horários comuns de cerimônia. `v-model` é sempre a string `HH:mm`. Mesmo racional do `DatePicker` — o nativo herdava o spinner do Windows/roda do iOS e ignorava o tema |
| `ColorPicker` | Seletor de cor headless (`ColorArea` + `ColorSlider` + `ColorField` do Reka) no lugar do `<input type="color">`, que abre o diálogo do sistema operacional — aparência diferente por SO, fora da paleta do produto e sem as ajudas que o casal precisa. Traz área de saturação/brilho, faixa de matiz, campo hexadecimal e uma faixa de cores sugeridas (as dos presets, que já passam no contraste). **`ColorAreaArea` não desenha fundo nenhum**: o gradiente clássico (matiz puro ao fundo, branco esmaecendo da esquerda, preto de baixo para cima) é responsabilidade de quem estiliza — sem ele o quadrado nasce branco e a área fica invisível. Espaço `hsb` com `saturation`/`brightness`, o par que casa com esse gradiente; com `lightness` no eixo Y o gradiente e o marcador não correspondem. `v-model` em `#rrggbb` |
| `Modal` / `Dialog` | Confirmações e formulários de criar/editar; prop `size` (`'md'` default, `'lg'` para conteúdo mais largo — ex.: lightbox de foto da galeria pública, wizard de convidado) e `scroll` (`'body'` default — o modal rola o slot inteiro; `'content'` — o slot recebe a altura disponível e controla a própria rolagem, para conteúdo com cabeçalho/rodapé próprios que não podem sair da vista). Altura sempre limitada a `calc(100dvh - 2rem)`: conteúdo maior que a janela rola dentro do modal, nunca estoura |
| `Tabs` | Headless via Reka UI (`TabsRoot`/`List`/`Trigger`/`Content`), estilo próprio (não um passthrough) — props `tabs: {id, label}[]` + `v-model`, conteúdo via slot nomeado por `id` (mesmo padrão de `Accordion`). Variantes: `boxed` (default — moldura compacta `inline-flex` sobre `bg-surface-muted`, ativo em cartão branco); `segmented` (largura cheia, abas dividindo a régua em partes iguais dentro de uma moldura única e a ativa **preenchida** com `bg-primary` — a aba deixa de ser filtro discreto e passa a ser a navegação principal da tela; em tela estreita a faixa rola na horizontal via `.no-scrollbar`). `segmented` é o que `/admin/[slug]/configuracoes` usa. Componente genérico (fica em `components/ui/`, não `components/admin/`) — reutilizável em qualquer tela, pública ou admin |
| `Toast` / `ToastViewport` | Feedback de ações assíncronas (nunca `alert()` nativo) — estado em `ui.store.ts` (`toasts`), disparado via `useToast().success()/error()/warning()/info()` (tom `warning` e duração por tom — 3500ms/5000ms/6000ms), `ToastViewport` montado uma vez em `app.vue`; toasts entram/saem com transição (`TransitionGroup` + `.transition-brand`) |
| `Chip` | Label + estado opcional de seleção (`selected`), toggle (`clickable`, emite `click` e expõe `aria-pressed` — no modo clicável ele é um toggle de verdade e o leitor de tela precisa do estado, não só do rótulo) e remoção (`removable`, emite `remove`) + slot `actions` para botões extras (ex.: editar) — único componente de "pill" da plataforma (categorias de presente, tags de convite, atalhos do wizard de convidados, valores sugeridos de contribuição) |
| `Badge` | Status visual (RSVP confirmado/pendente/recusado) |
| `Card` | Contêiner padrão para itens de lista (convidado, presente); props `radius` (`'xl'` = tratamento premium, **default da plataforma inteira**; `'lg'` = degrau reduzido, só onde cartões densamente empilhados fariam o raio/sombra grandes competirem entre si), `elevation` (`'xl'` default, acompanha `radius="xl"`; `'sm'` no degrau reduzido) e `variant` (`'default'` | `'interactive'` — hover `shadow-md`/`border-primary` no degrau médio | `'highlight'` — leve ênfase `bg-primary/[0.03]` para cards de destaque, ex. prazo de RSVP no dashboard | `'plain'` — sem moldura nenhuma (borda/fundo/sombra/raio), só a estrutura header/corpo/footer, para quando o contêiner pai já é a moldura: um card dentro de um modal) |
| `Avatar` | Representação visual de convidado/casal |
| `Skeleton` | Estado de carregamento consistente |
| `EmptyState` | Estado vazio ilustrado e padronizado; prop opcional `icon` (ícone lucide acima do título). Reaproveitado também para estado de **erro** de carregamento (título/descrição de erro + botão "Tentar novamente" no slot default chamando `refresh()`) — não é um componente próprio de erro, é o mesmo `EmptyState` com conteúdo diferente |
| `SectionDivider` | Ornamento linha–ponto–losango–ponto–linha, tingido na cor primária do tema (`primary/30`–`primary/60`); puramente decorativo (`aria-hidden`), sem conhecimento de domínio — usado por `PublicEditorialSection`, sempre abaixo do título |
| `Accordion` | Headless via Reka UI (`AccordionRoot`/`Item`/`Header`/`Trigger`/`Content`), `type="single" collapsible`; navegação por teclado e `aria-expanded` nativos do primitive — usado pela seção de FAQ pública. Slot com escopo `#content="{ item }"` (opcional) permite conteúdo rico por item além do texto simples de `item.content` — usado nas mensagens do site em `/admin/[slug]/configuracoes` e nas coordenadas opcionais em `/admin/cronograma`. Variantes: `card` (default — o acordeão premium do público: `rounded-xl`, `shadow-sm`, chevron num disco `bg-primary/10`); `plain` (a linha de configuração: `rounded-md` de 1px, título com linha de apoio `item.hint`, chevron solto e `bg-surface-muted/40` quando aberto — vários itens empilhados numa seção sem que cada um pareça um cartão independente). Prop `defaultOpenId` abre um item no primeiro render |
| `CountdownTimer` | Contagem regressiva até a data/hora do evento (dias/horas/minutos/segundos); sem conhecimento de domínio (props `targetDateTime`, `variant` — `'cards'` default/caixas, `'inline'` números soltos com separador —, slot `past` para a mensagem de "já aconteceu") — usado no Hero público (`variant="inline"`, condicionado a `config_tema.showCountdown`) e no dashboard admin (`variant="cards"` default, sempre visível) |

### 3.3 Regras de governança

- Nenhum estilo visual (cor, espaçamento, tipografia) é definido diretamente em componentes de domínio — sempre via classes Tailwind mapeadas aos tokens, ou via componente de `components/ui/`.
- Toda nova variante visual passa primeiro pelo Design System antes de ser usada em uma feature específica — proibido criar "botão especial" isolado dentro de uma página.
- Temas por casamento são dados armazenados em `casamentos.config_tema` (jsonb — exclusivamente atributos visuais, nunca comportamento de negócio como `modo_lista_convidados`, ver [`PRODUCT.md`](PRODUCT.md)), aplicados via CSS variables no layout público. Shape atual: `{ presetId?: string, primaryColor: string, secondaryColor: string, titleColor?: string, bodyColor?: string, ornamentColor?: string, fontPairId: string, headingStyle?: 'classic' | 'engraved', ornamentFrame?: boolean, sectionOrder?: string[], coverImageUrl?: string, storyImageUrl?: string, monogramImageUrl?: string, coverFocalX?: number, coverFocalY?: number, storyFocalX?: number, storyFocalY?: number, showCountdown: boolean, heroButtons?: string[], heroFeaturedButton?: string }` (chaves do JSON permanecem em inglês, camelCase — só o nome da coluna que guarda o JSON mudou). **As chaves gravadas por `server/api/wedding/theme.patch.ts` são derivadas do próprio `themeConfigSchema`** (`Object.keys(schema.shape)`), então campo novo do schema passa a ser persistido no mesmo commit em que nasce. A lista enumerada à mão que existia ali — e que descartou campo em silêncio duas vezes (`docs/CHANGELOG.md`) — foi removida na Fase Rebrand do Convite; `presetId` é a única chave com tratamento próprio (string vazia vira `undefined`). O que continua exigindo atenção é o inverso: chave gerida por outro endpoint **não pode** entrar no schema, e é essa ausência que a preserva no merge. `presetId` é só um rótulo informativo do último preset aplicado (ou `'custom'` após qualquer edição manual) — nunca usado para resolver a aparência em si, que sempre lê `primaryColor`/`secondaryColor`/`fontPairId` diretamente.
- `config_tema` é gerenciado por um endpoint próprio (`PATCH /api/wedding/theme`, `shared/schemas/theme.ts`), separado dos dados de negócio do evento (`PATCH /api/wedding`, `shared/schemas/wedding.ts`). `coverImageUrl`/`storyImageUrl`/`monogramImageUrl`/`dressCodeImageUrl` ficam de fora até desse schema: são geridos exclusivamente pelos respectivos endpoints de upload/remoção (`cover-upload`/`story-upload`/`monogram-upload`/`dress-code-upload`), nunca submetidos junto com o restante do formulário de Aparência, evitando que salvar cor/fonte apague uma imagem por engano. As quatro imagens são **independentes** — arquivos próprios no mesmo bucket `wedding-covers` (`{casamento_id}/cover.{ext}`, `{casamento_id}/story.{ext}` e `{casamento_id}/monogram.{ext}`), cada uma com seu próprio par de endpoints e composable (`useWeddingCoverUpload`, `useWeddingStoryUpload`, `useWeddingMonogramUpload`, `useWeddingDressCodeUpload`). O monograma é o único sem editor de corte e sem ponto de foco: é uma marca pronta, entregue no enquadramento em que deve ser usada, então recortá-la só ofereceria uma decisão inexistente e um jeito de estragar a arte. SVG fica fora da allowlist de MIME mesmo sendo o formato natural de uma marca — é um documento que pode carregar `<script>`, servido do mesmo domínio do site; PNG/WebP com transparência resolvem o caso real.
- A paleta do casal é sempre **duas cores** (`primaryColor` + `secondaryColor`, cada uma validada independentemente pela seção 3.4), sempre editáveis por hexadecimal exato. `shared/theme-presets.ts` cataloga temas prontos (`THEME_PRESETS`, cor+cor+par tipográfico combinados) e pares tipográficos (`FONT_PAIRS`, independentes de cor) — presets são só um atalho de largada: escolher um preenche os três campos de uma vez, mas cada um continua editável manualmente depois, e a fonte é sempre uma escolha independente da cor.
- **Personalização avançada**: além da paleta primária/secundária, o casal pode opcionalmente sobrescrever `titleColor`/`bodyColor` — resolvidos para `--color-heading`/`--color-body` (`useWeddingTheme.ts`), tokens que, sem sobrescrita, herdam `--color-text`. É um toggle "Personalização avançada" na tela de Aparência (`app/pages/admin/configuracoes/index.vue`): desligá-lo limpa os dois campos no submit seguinte, em vez de deixar um valor escondido e não-editável. Cada cor é validada por contraste independentemente, como `primaryColor`/`secondaryColor`. Adoção pelos componentes é incremental — `text-heading`/`text-body` (utilities geradas a partir dos tokens) substituem `text-text` onde fizer sentido, não em todo o código de uma vez. Na variante *com* foto de capa do Hero, o texto permanece branco fixo (legibilidade sobre a imagem) — a cor de título do casal não se aplica ali, decisão deliberada.
- `shared/theme-presets.ts` inclui o preset `borgonha-editorial` ("Borgonha Editorial" — Borgonha profundo `#5c1a2b` + Dourado fosco `#8a6a1f`, par `DM Serif Display + DM Sans`).
- **Ferramenta de enquadramento (ponto de foco)**: toda foto que é cortada em proporção fixa (grade da galeria em `aspect-square`, foto da "Nossa História" em `aspect-[4/5]`, foto de capa em `object-cover` de altura de viewport) pode ter seu ponto de foco escolhido na edição da foto (galeria) ou no upload (capa/história), em vez de sempre cortar pelo centro. `ImageFocalPointPicker.vue` (`components/ui/`) é o componente compartilhado — área de seleção mostra a foto **inteira, sem cortar** (clique/arraste mapeia 1:1 para as coordenadas reais da imagem), com um bloco secundário mostrando a prévia do corte real. Aceita teclado (setas, passos de 5%) além de ponteiro. Usado em 3 pontos: `PhotoGalleryManager.vue` (campos `fotos.foco_x`/`foco_y`), `CoverImageUploader.vue`/`StoryImageUploader.vue` (campos `config_tema.coverFocalX`/`coverFocalY`/`storyFocalX`/`storyFocalY`, endpoint próprio `PATCH /api/wedding/theme/focal-point`). Persistência é debounced (400ms) no client; valor default ausente = 50/50 (centro). Um upload novo de capa/história sempre reseta o foco da imagem anterior. Na galeria, o foco é preservado entre syncs pela chave `id_arquivo_origem`; reenviar o mesmo arquivo no Drive gera um `fileId` novo e perde o foco salvo (limitação conhecida).
- `config_conteudo` é uma coluna irmã de `config_tema`, não uma extensão dela: guarda as mensagens narrativas do site público (boas-vindas, versículo, história, dress code, manual do convidado, manual dos padrinhos, intro de presentes, FAQ), nunca atributos visuais — endpoint próprio (`PATCH /api/wedding/content`, `shared/schemas/content.ts`), nunca misturado ao formulário de Aparência.

### 3.4 Validação automática de contraste

Ao salvar `config_tema`, `primaryColor`, `secondaryColor` e, quando definidas, `titleColor`/`bodyColor` são validadas independentemente contra `--color-surface`/`--color-text` calculando a razão de contraste (fórmula de luminância relativa do WCAG, `shared/utils/contrast.ts#checkColorContrast`). Se qualquer uma ficar abaixo de 4.5:1, a interface administrativa bloqueia o salvamento — evitando que a customização visual quebre a acessibilidade prometida na seção 5. Todo preset de `shared/theme-presets.ts` é coberto por teste de guarda garantindo que as duas cores de cada entrada passam nesse mínimo.

Reprovar não basta: `suggestAccessibleColor()` (mesmo arquivo) devolve o tom mais próximo que passa, **escurecendo só a luminosidade e preservando matiz e saturação** — a correção precisa devolver a cor que o casal escolheu, um pouco mais fechada, não uma cor diferente. É isso que permite ao aviso oferecer a saída em um clique.

A mensagem de erro é **texto de interface**, não diagnóstico técnico: não cita a razão de contraste, o limiar do WCAG nem seção de documentação interna (a versão antiga trazia os três, incluindo uma referência a `CLAUDE.md` vazando para a tela do produto). Vive em constante única no `shared/schemas/theme.ts`, porque vale para as quatro cores do tema e para a cor de grupo, e usa a mesma redação do `ContrastHint` — a tela e o erro de salvar não devem falar línguas diferentes.

#### A exceção do ornamento

`ornamentColor` é a **única** cor de `config_tema` que não passa por contraste — valida só o formato hexadecimal (`ornamentHexColorSchema`). Não é uma brecha: é o reconhecimento de que a régua estava medindo a coisa errada. O dourado de um convite fica em ≈2,3:1 contra o marfim e seria reprovado como cor de texto, corretamente — como texto ele é ilegível mesmo. Mas ele nunca é texto: pinta filete, divisor, moldura e monograma, e o WCAG 1.4.11 exclui explicitamente do requisito de contraste o que é puramente decorativo.

O que sustenta a exceção é o que **não** pode acontecer: nenhum componente usa `--color-ornament` em texto de corpo, título ou rótulo de botão. Um teste de guarda em `tests/unit/shared/schemas/theme.spec.ts` fixa os dois lados — o mesmo dourado que passa como `ornamentColor` continua reprovado como `bodyColor`.

A única aparição do ornamento como texto é a seção **Versículo**, e ali o fundo é a cor primária escura, não o marfim — é esse par que precisa ser legível. Quem mede é `checkOrnamentOnPrimary()`: no admin ele vira aviso (não bloqueio — o casal pode ter razões para o dourado exato do convite dele), e sobre os presets da plataforma ele é obrigatório, garantido em `tests/unit/shared/theme-presets.spec.ts`. Foi por isso que o preset "Convite de Luxo" saiu com `#cbaa71` em vez do `#c8a56a` do brief: dois pontos de luminosidade acima levam o par de 4,41:1 para 4,65:1 sem diferença perceptível lado a lado.

As cores da **paleta do Manual dos Padrinhos** (`config_conteudo.groomsmenManual.palette`) também não passam por contraste, por um motivo diferente: ali a cor não pinta interface nenhuma, ela é o conteúdo — a amostra do tom do vestido que a madrinha vai comprar. Exigir 4,5:1 proibiria justamente o champanhe e o rosé que um casal quer mostrar. O que carrega a informação para quem não distingue o tom é o **nome obrigatório** ao lado de cada amostra, não a bolinha.

## 4. Componentes Reutilizáveis

### 4.1 Componentes de domínio compartilhados

| Componente | Uso |
|---|---|
| `GuestCard` | Exibição de um convidado (nome, status RSVP, ações rápidas) — usado em listagens e dentro de grupos |
| `GroupTree` | Visualização hierárquica de grupo → convidados |
| `RsvpForm` | Formulário de confirmação de presença, reutilizado no fluxo individual e em grupo |
| `GiftCard` | Exibição de um item da lista de presentes, com estado reservado/disponível |
| `GiftReservationModal` | Fluxo de reserva de presente |
| `StatusBadge` | Badge de status genérico, parametrizado por mapa de cores/labels (RSVP, convite enviado, etc.) |
| `ProgressSummary` | Barra/cartão de progresso (ex: "82 de 120 confirmados") reutilizado no dashboard e em relatórios |
| `CsvImportWizard` | Fluxo de importação de convidados em etapas (upload → mapear colunas → revisar → confirmar) |
| `EditorialSection` | Wrapper padrão de "capítulo" da home pública — prop `eyebrow` (rótulo curto, uppercase, tracked, cor `primary/60`) + título centralizado + `SectionDivider` (nessa ordem: eyebrow → título → divisor), alternância de fundo `bg-surface`/`bg-surface-muted`/`bg-secondary/10` (prop `tone`, aplicada de forma consistente em toda seção pública — nunca ad hoc), reveal-on-scroll via `v-motion`, `id` para âncora de navegação. Toda seção da home o reutiliza |
| `VenueMap` | Embed do Google Maps num `<iframe>` (SSR-safe, sem manipulação de `window`/DOM, sem chave de API) — props `query` (coordenadas ou endereço em texto, montada por `shared/utils/mapa-local.ts`), `label` e `size` (`sm` = preview de confirmação do painel, `md` = mapa do site público). Vive em `components/ui/` desde a Fase Localização, quando passou a aparecer nos dois contextos (`EventSpotlight.vue` no site, `AdminLocationField` no painel) — antes era `components/public/VenueMap.vue`. Aparece sempre que há local/endereço ou coordenadas; sem nenhum dos dois, nem mapa nem botão "Ver no mapa" aparecem |
| `AdminSection` (`components/admin/`) | Wrapper padrão de página do admin — slots `title`/`description` (props string, não slot) + `actions` (a barra de ferramentas do cabeçalho: filtro de texto seguido do CTA primário) + slot default para o conteúdo |
| `AdminColumnFilter` (`components/admin/`) | O painel de filtro de uma coluna: cabeçalho com o nome da coluna e "Limpar" (desabilitado quando não há nada aplicado), seção "Ordenar" e seção "Filtrar" (texto, lista de opções ou caixas de marcar quando a coluna é de múltipla escolha). É só o painel — quem o põe num popover é `AdminTable` (cabeçalho, do `md` pra cima) e quem o empilha inline é `AdminTableFilterBar` (modal do celular), pra que os dois contextos nunca divirjam. Filtro de lista fechada é desenhado como lista de opções aqui dentro, **não** como `UiSelect`: o dropdown do Reka é portalado pro `body`, e abri-lo dentro do popover contaria como clique fora, fechando o filtro na primeira interação |
| `AdminTableFilterBar` (`components/admin/`) | Chips removíveis do que está filtrado + "Limpar tudo" + o botão "Filtros" (só abaixo de `md`) que abre o modal com todos os painéis de coluna |
| `GuestSectionNav` (`components/admin/guests/`) | Menu da seção Convidados — Visão organizada / Modo lista / Importar-Exportar, mais Grupos em "Gerenciar". Coluna do `lg` pra cima, fileira rolável na horizontal abaixo disso (nunca um segundo drawer: dois drawers disputariam o mesmo gesto). "Importar / Exportar" aponta para a Visão organizada com o importador aberto (`?importar=1`) — é ação, não tela, e sem um alvo de verdade o item seria decoração |
| `GuestListDraftPanel` (`components/admin/guests/`) | O rascunho da lista ("Em consideração") como faixa horizontal de fichas abaixo da tabela, com o aviso de que essas pessoas não entram em contagem, não recebem convite e não aparecem no site |
| `AdminStatCard` (`components/admin/`) | Cartão de métrica do dashboard — ícone lucide + label + valor + `tone` opcional (`default`/`primary`/`success`/`warning`/`danger`), valor truncado com `title` para o texto completo no hover |
| `AdminQuickAction` (`components/admin/`) | Atalho de ação do dashboard — ícone + label + link (`NuxtLink`), mesma linguagem visual de `Card` `variant="interactive"` mas construído à parte por não ser um `Card` (é sempre um link) |
| `AdminLocationField` (`components/admin/location/`) | O campo de localização do cronograma inteiro, em três modos: busca (autocomplete), confirmação (nome + endereço + preview pequeno + "Ver no mapa" / "Alterar local" / "Remover local") e cadastro manual. Nunca exibe latitude/longitude. Compõe `AdminLocationSearchBox`, `AdminLocationManualForm` e `AdminLocationMapPicker` |
| `AdminLocationSearchBox` (`components/admin/location/`) | Autocomplete de lugares — sugestão de duas linhas (nome + endereço que a diferencia), navegação por seta/Enter/Esc, debounce de 300ms. Lista vazia nunca é erro: é o gatilho do cadastro manual |
| `AdminLocationManualForm` (`components/admin/location/`) | Endereço em partes (nome, logradouro, número, complemento, cidade, UF) + prévia de como o endereço aparecerá no site. Sem campo técnico algum |
| `AdminLocationMapPicker` (`components/admin/location/`) | Modal com mapa arrastável (Leaflet + tiles do OpenStreetMap, `.client.vue`) para posicionar o marcador — "Posicione o marcador no local correto" / "Confirmar localização". Único lugar do produto que usa Leaflet, e só no painel: o site público continua com o embed sem chave |

### 4.2 Critério para "promover" um componente a reutilizável

Um componente só é extraído para uso compartilhado após aparecer em **pelo menos 2 contextos reais** — evita abstração prematura sobre necessidades hipotéticas.

## 5. Responsividade

- **Mobile-first** obrigatório: a maioria dos convidados acessará o link de RSVP diretamente do WhatsApp no celular.
- Breakpoints padrão do Tailwind (`sm`, `md`, `lg`, `xl`) — sem breakpoints customizados salvo necessidade comprovada.
- Painel administrativo é otimizado primeiro para desktop/tablet (uso típico do casal planejando em casa), mas nenhuma tela pode "quebrar" em mobile — no mínimo, uso funcional garantido. A navegação do admin é uma sidebar (`layouts/admin.vue`, estado `sidebarOpen` em `ui.store.ts`): no desktop, colapsa para uma trilha só de ícones; no mobile, vira um drawer sobreposto (com overlay escurecido) que começa fechado e fecha sozinho ao navegar para outra página.
- Tabelas administrativas em telas estreitas colapsam para formato de cards empilhados, em vez de scroll horizontal forçado como única solução.
- Imagens (galeria do casal, fotos de presentes) sempre com `srcset`/componente de imagem otimizada do Nuxt (`<NuxtImg>`), nunca `<img>` cru com imagem em resolução total — exceção deliberada: fotos da galeria, servidas direto do Google Drive (ver `docs/CHANGELOG.md`).

## 6. Acessibilidade

- Meta: conformidade com **WCAG 2.1 nível AA**.
- Uso de componentes headless acessíveis (Reka UI) como base para Modal, Dialog, Combobox, garantindo gerenciamento correto de foco e navegação por teclado.
- Contraste mínimo de 4.5:1 entre texto e fundo, validado para toda combinação de tema customizado pelo casal (ver seção 3.4).
- Todo elemento interativo é acessível via teclado (`Tab`/`Enter`/`Space`), sem exceções para componentes customizados.
- Formulários (RSVP, cadastro de convidado) com `label` associado a cada campo, mensagens de erro anunciadas via `aria-live`.
- Imagens decorativas com `alt=""`; imagens de conteúdo (fotos do casal) com `alt` descritivo preenchido pelo casal.
- Testado com leitor de tela (NVDA/VoiceOver) nos fluxos críticos antes de cada release maior.

## 7. SEO

- Site público renderizado via **SSR** — essencial para que links compartilhados no WhatsApp gerem preview correto (Open Graph) e para indexação eventual em buscadores.
- Meta tags dinâmicas por casamento: `title`, `description`, `og:image` (foto de capa do casal), geradas via `useSeoMeta`/`useHead` do Nuxt a partir dos dados de `weddings`.
- URLs amigáveis: `meusitecasamento.com/{slug}` como página pública principal (`slug` único por casamento, definido na criação — não há UI de edição pelo casal hoje).
- `robots.txt` e `sitemap.xml` gerados automaticamente para o domínio público; painel administrativo (`/admin/**`) sempre `noindex, nofollow`.
- Páginas de RSVP individuais (`/rsvp/{code}`) são `noindex` — contêm identificador único e não devem ser indexadas.

## 8. Performance (frontend/visual)

- Imagens sempre servidas via `<NuxtImg>`/`<NuxtPicture>` com formatos modernos (`webp`/`avif`) e lazy loading fora do viewport inicial.
- **`sizes` do NuxtImg exige formato por breakpoint**: todo uso de `sizes` com unidade `vw` em `NuxtImg`/`NuxtPicture` neste projeto precisa listar os 5 breakpoints explicitamente (`@nuxt/image` não aceita a sintaxe crua do HTML — `sizes="100vw"` sozinho silenciosamente gera um `srcset` de ~1-2px de largura, sem nenhum erro visível), ex.: `sizes="sm:100vw md:100vw lg:100vw xl:100vw 2xl:100vw"` (constante) ou `sizes="sm:100vw md:50vw lg:50vw xl:50vw 2xl:50vw"` (variável — a biblioteca desloca cada valor para valer "a partir deste breakpoint até o próximo", não "abaixo deste breakpoint"; sempre conferir o `srcset`/`naturalWidth` renderizado após qualquer mudança). Valores em `px` (ex.: `sizes="400px"`, usado em `GiftCard.vue`) não têm esse problema. Aplica-se a capa/história (`Hero.vue`, `StorySection.vue`) e qualquer novo uso de `NuxtImg`/`NuxtPicture`; não se aplica à Galeria, que usa `<img loading="lazy">` direto do Google. Achado real documentado em `docs/CHANGELOG.md`.
- Dados do painel administrativo paginados no servidor (nunca carregar lista completa de 500 convidados de uma vez) — paginação ou scroll infinito com limite de página razoável (ex: 25–50 itens).
- Uso de `useAsyncData` com chaves de cache adequadas para evitar refetch desnecessário ao navegar entre páginas do admin.
- Bundle do painel administrativo carregado separadamente do bundle do site público (via code-splitting natural de rotas do Nuxt) — um convidado nunca baixa código do admin. O SDK do Supabase (client-side) também é carregado sob demanda, só em `/admin/**` e `/login` (`app/plugins/supabase-auth.client.ts`) — o site público nunca paga o custo desse bundle nem faz as chamadas de auth associadas a ele.

---

*Este documento evolui junto com `components/ui/`, `shared/theme-presets.ts` e `shared/hero-buttons.ts`. Qualquer token/componente novo do Design System deve ser refletido aqui.*
