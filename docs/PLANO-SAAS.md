# Plano de Execução — Remodelagem SaaS

> Checklist de trabalho derivado da Auditoria Arquitetural de 2026-08-21 (documento completo publicado como artifact: https://claude.ai/code/artifact/7ab3bd5d-6a51-49d2-9ba7-abfd26ab6752). Este arquivo é a fonte de verdade de **progresso** — atualizado a cada item concluído, com entrada correspondente no "Log de progresso" ao final. `docs/ROADMAP.md` continua sendo a fonte de verdade de **fase de produto** (Fase 4/5); este documento é o detalhamento tático de como chegar lá.

**Como usar**: marcar `[x]` só quando o item estiver de fato implementado e validado (não necessariamente mergeado — ver contrato de execução abaixo). Cada passo bloqueia o próximo, na ordem abaixo (ver seção 15/17 do artifact para a justificativa do sequenciamento).

## Contrato de execução (definido pelo usuário em 2026-08-21)

- **Execução contínua e autônoma**: sem pausar para pedir autorização de edição, commit, PR ou decisão técnica razoável dentro do escopo do plano. Sem apresentar progresso parcial no meio do caminho.
- **Validação a cada etapa**: lint/typecheck/testes unitários/integração/E2E/build/validações de banco e migration/busca de referências antigas/imports quebrados/arquivos órfãos, conforme aplicável. Falha → investigar, corrigir, revalidar, só then seguir.
- **Commits**: unidades coerentes de trabalho, sem pedir aprovação. Nem um commit por arquivo, nem um commit gigante por Passo inteiro.
- **PRs**: um PR por unidade coerente de responsabilidade (pode passar de 20 no total). PRs dependentes usam o modelo *stacked* (branch parte da branch do PR anterior, nunca de `main` direto, quando há dependência real).
- **Merge para `main` é proibido durante toda a execução autônoma** — `main` dispara deploy automático de produção. PRs ficam criados e prontos, nunca mergeados, até autorização explícita no checkpoint final.
- **Único checkpoint humano**: ao final de todo o plano (todos os Passos + correções de documentação), com relatório de PRs, ordem de merge recomendada, validações e pendências reais.
- Rename para português (Passo 1) é tratado como migração arquitetural completa — banco, backend, APIs, frontend, testes, documentação, configs — nunca parcial. Busca sistemática por referências aos nomes antigos antes de considerar concluído.

---

## Status geral

- **Fase atual**: Passo 1 **completo, mergeado em `main` e promovido para produção**. Passo 2 (suíte de testes de isolamento) **completo, validado e mergeado**. Passo 3 (Conta → Casamento) **completo** — contexto multi-membership, endpoints de gestão de membros, rotas `/admin/{slug}/**`, tela de seleção de casamento, UI de colaboradores e E2E, tudo implementado e validado; PR #87 aberto, aguardando checkpoint de autorização pra merge. Passo 4 **parcialmente implementado, validado e mergeado** — itens 1-3 já vinham do Passo 1, item 4 (RLS das tabelas de SaaS-readiness) validado contra `dev` e `prod`; itens 5-6 deliberadamente não iniciados (sem call site real ainda). Passo 8 (fundação do painel interno da plataforma) **completo e validado** — `/plataforma`, tabela `operadores_plataforma`, endpoint único de leitura entre tenants; branch empilhada sobre a do Passo 3 (mesmos arquivos de auth/middleware), aguardando o mesmo checkpoint.
- Executados em sequência sob autorização explícita do usuário ("Realizar Passos 2, 3, 4 / Sem interrupção") em 2026-08-21; merge e promoção pra prod autorizados em 2026-08-24 ("faça o merge e push") — ver Log de progresso para detalhes técnicos de cada um.
- **Merge (2026-08-24)**: os 4 PRs (#83 hotfix → #84 Passo 2 → #85 Passo 3 → #86 Passo 4) foram mergeados em `main` nessa ordem. #84 exigiu 4 rodadas de correção do gate de integração do CI antes de ficar verde (primeira execução real do workflow, achados reais documentados no Log de progresso) — nenhuma delas afeta `dev`/`prod`, só o Postgres descartável do GitHub Actions.
- Banco, `shared/`, `server/`, `app/` (types/composables/components/pages), `tests/unit/**` (57 arquivos) e documentação (`CLAUDE.md`, `docs/DATABASE.md`, `docs/ARCHITECTURE.md`, `docs/PRODUCT.md`, `docs/DESIGN-SYSTEM.md`, `docs/ROADMAP.md`) traduzidos e consistentes desde o Passo 1. `npm run typecheck`/`lint`/`test`/`build` limpos (0 erros, 384/384 testes unitários passando após Passo 2).
- **Merge e produção do Passo 1 (2026-08-21)**: os 4 PRs da pilha (#79→#80→#81→#82) foram mergeados em `main` nessa ordem, com autorização explícita do usuário. As 5 migrations de rename foram aplicadas ao projeto `prod` (`elatoqglxrpqriqphkjy`) logo em seguida. Deploy do Vercel do commit final (`9ef60ef`) concluído com sucesso; usuário confirmou produção funcionando normalmente.
- **Migrations do lote Passos 2-4 promovidas pra `prod` (2026-08-24)**: `20260821110001` (fix wizard de convidado) e `20260821120001` (RLS SaaS-readiness) aplicadas com sucesso via `supabase db push --linked` contra `elatoqglxrpqriqphkjy`. `supabase migration list --linked` confirma local == remote em toda a lista, sem gaps. O bug do wizard de criação de convidado, ao vivo em produção desde o Passo 1, está corrigido.
- **Última atualização**: 2026-08-25.
- **Decisão confirmada com o usuário em 2026-08-21**: composables (`useGuests`, `useInvites`...) e pastas de componente (`components/gifts/` etc.) **permanecem em inglês** — mesmo raciocínio já aplicado às pastas de rota de `server/api/**` (organização interna de código/protocolo, não vocabulário de dados do domínio). Os dois itens correspondentes abaixo ficam marcados como decisão consciente, não pendência.

### Bloqueio anterior — resolvido

O bloqueio de validação (sem Docker local, `supabase db push` inicialmente negado pelo classificador) foi resolvido: o usuário rodou `npx supabase db push --linked` diretamente no terminal dele, e nas tentativas seguintes o comando passou a ser permitido também para mim. Três bugs reais foram encontrados e corrigidos só porque essa validação existiu (nenhum apareceria em revisão estática de SQL):
1. `UPDATE` de tradução de enum rodando antes do `DROP` do `CHECK` antigo (violava a própria constraint ainda ativa).
2. Trigger de consistência disparando durante o `UPDATE` de `pagamentos_presentes.tipo`, chamando o corpo *antigo* da função (`new.kind`, campo que não existe mais) — mesma classe de bug que este projeto já teve historicamente com `reserve_gift`.
3. `CREATE OR REPLACE FUNCTION` não permite renomear parâmetro de entrada — corrigido trocando para `DROP FUNCTION` + `CREATE FUNCTION` nas 9 funções de negócio sem dependentes por OID; `is_membro_casamento`/`is_dono_casamento` mantêm o parâmetro `p_wedding_id` de propósito (têm ~90 RLS policies dependentes por OID).

## Decisões já validadas com o usuário

- [x] Ambiente confirmado pré-lançamento, sem dado real de casamento em produção (2026-08-21).
- [x] Remodelagem completa da nomenclatura do banco para português aprovada, mesmo contra a recomendação técnica original da auditoria (2026-08-21).

---

## Passo 1 — Rename completo para português

Corte coordenado único (banco + API + tipos + schemas + testes + docs), validado inteiramente em `dev` antes de promover. Absorve as correções P0 da auditoria no mesmo movimento.

- [x] Migration: renomear as 27 tabelas (`20260821090001`)
- [x] Migration: renomear colunas centrais (`wedding_id`→`casamento_id` em toda tabela filha, etc.) (`20260821090001`)
- [x] Migration: corrigir `group_id`→`convite_id` em `gift_reservations`/`gift_contributions` (renomeadas para `reservas_presentes`/`contribuicoes_presentes`) (`20260821090001`)
- [x] Migration: recriar trigger de consistência `casamento_id`/`convite_id` ausente em `companions` (→ `acompanhantes_avulsos`) (`20260821090003`)
- [x] Migration: recriar trigger de consistência ausente em `photos.source_connection_id` (→ `fotos.conexao_id`) (`20260821090003`)
- [x] Migration: traduzir valores de `CHECK`/status (`20260821090002`)
- [x] Migration: renomear RLS policies (`20260821090004`)
- [x] Migration: renomear/recriar funções Postgres (`20260821090003`) — inclui limpeza dos 2 overloads obsoletos de `reserve_gift`
- [x] Migration: adicionar `is_slug_reservado(text)` + `CHECK` em `casamentos.slug` (`20260821090005`)
- [x] Migration: escopo de conta em `assinaturas`/`funcionalidades_habilitadas` + `planos.max_casamentos` + ciclo de vida (`20260821090005`)
- [x] Validar as 5 migrations acima contra Postgres real (projeto `dev`, via `npx supabase db push --linked`) — 4 bugs reais encontrados e corrigidos (ver "Bloqueio anterior — resolvido")
- [x] Trocar `service_role` por `serverSupabaseClient` em `wedding/gallery/connection.delete.ts` e `wedding/gallery/sync.post.ts` — só nas leituras/escritas que resolvem a conexão do próprio casal (RLS como defesa em profundidade); `syncGalleryConnection()` em si continua em `service_role` de propósito, por ser compartilhado com o cron (sem sessão de usuário)
- [x] Renomear policies de storage (migration `20260821100001`) — as 9 policies de `storage.objects` dos buckets `wedding-covers`/`wedding-photos`/`wedding-event-segments` foram renomeadas via `DROP`+`CREATE` (não `ALTER POLICY RENAME` — `storage.objects` pertence a `supabase_storage_admin`, e o role de migration só tem privilégio de `CREATE`/`DROP` policy nela, não `ALTER`/`RENAME`, achado real desta sessão). Validado e aplicado em `dev` e `prod`
- [x] Regenerar `database.types.ts` a partir do schema novo (`npx supabase gen types typescript --linked`, confirmado sem nomes antigos)
- [x] Traduzir `shared/schemas/**` e `shared/utils/**` (campos que espelham colunas do banco) — branch `refactor/pt-br-shared-e-utils`
- [x] Traduzir `server/utils/**` (todas as chamadas `.from`/`.select`/`.eq`/`.rpc`) — mesma branch
- [x] Traduzir `server/api/**` (76 endpoints, todos os domínios) — branch `refactor/pt-br-server-api`. `npm run typecheck`/`lint` limpos em `shared/`+`server/` inteiro
- [x] Renomear pastas/arquivos de rota em `server/api/**` (ex.: `invites/`→`convites/`) — **decisão: NÃO fazer** — nomes de pasta de rota não são "nomenclatura do domínio" visível, e renomear quebraria URLs de API já em uso sem nenhum ganho real (as rotas HTTP não são a "nomenclatura em inglês" que o briefing original pedia para traduzir — o conteúdo/campos já estão traduzidos). Decisão consciente, não pendência esquecida.
- [x] Renomear composables (`useInvites`→`useConvites` etc.) — **decisão confirmada com o usuário em 2026-08-21: NÃO fazer**, mesmo raciocínio do item acima (organização interna de código, não vocabulário de dados). O conteúdo interno de cada composable (campos que acessam objetos vindos do banco) já está 100% traduzido.
- [x] Renomear pastas de componente (`components/gifts/`→`components/presentes/`) — **mesma decisão acima: NÃO fazer**. `components/rsvp/` também mantém (RSVP é termo universal, nem entraria nessa discussão).
- [x] Atualizar `app/types/**` derivados (`Guest`, `Wedding`, `GiftPayment`, `auth.ts#WeddingContext`, etc.) — branch `refactor/pt-br-app-layer`. `WeddingContext.weddingId/role/memberId` deliberadamente mantidos (tipo transversal, tradução adiada, baixo risco)
- [x] Traduzir `app/composables/**` (31 arquivos + 2 stores) e `app/components/**`/`app/pages/**`/`app/layouts/**`/`app/utils/**` — campos que acessam objetos vindos do banco atualizados para os novos nomes; nomes de função/arquivo/pasta mantidos em inglês (ver decisão acima). `npm run typecheck`/`lint` limpos em `app/` inteiro (0 erros)
- [x] Atualizar os 57 testes unitários existentes para os novos nomes — 22 arquivos precisavam de correção (mocks/fixtures com campos antigos), 35 já estavam corretos. `npm run test` limpo (383/383 passando)
- [x] Atualizar `docs/DATABASE.md`, `docs/ARCHITECTURE.md`, `docs/PRODUCT.md`, `docs/DESIGN-SYSTEM.md`, `docs/ROADMAP.md` e `CLAUDE.md` (seção 6 "Idioma" estava desatualizada — dizia "identificadores em inglês", o oposto da decisão já aprovada) com a nomenclatura nova
- [x] Atualizar `supabase/seed.sql` para os novos nomes

### Escopo deliberadamente deixado em inglês nesta etapa (registrar, não esquecer)
- Nomes de constraint de FK/PK no banco (identificadores internos de catálogo, sem ganho funcional em renomear).
- Chaves internas de `config_tema`/`config_conteudo` (JSONB — não são colunas).
- Campos técnicos de integração com Google Drive (`code`, `folderId`, `folderName`) e da API externa da InfinitePay (`order_nsu`, `redirect_url` etc. — contrato de terceiro, não nosso).
- Alguns DTOs locais de baixo risco (ex.: `AuditLogInput.entityType/entityId`) — a escrita no banco já está 100% correta; o nome do parâmetro TS é cosmético e pode ser revisitado depois sem risco.
- DTOs computados/agregados de view-model (`PublicGift`, `RsvpInvitePayload.wedding.coupleNames/eventDate`, `GiftReservationEntry`/`GiftContributionEntry`/`GiftActivityEntry`, `DashboardSummary`, `InviteListItem` — campos calculados/agregados, não colunas de tabela) — exceção documentada e consistente nos dois lados (server e client nunca misturam os dois padrões no mesmo objeto).
- Nomes de composable (`useGuests`, `useInvites`...) e pastas de componente (`components/gifts/` etc.) — decisão confirmada com o usuário em 2026-08-21, mesmo raciocínio das pastas de rota de `server/api/**`.
- `app/types/auth.ts#WeddingContext.weddingId/role/memberId` — tipo transversal, tradução adiada por baixo risco/baixo ganho.

### Pendências reais de Passo 1 (fora do rename em si)
- [ ] Gate de CI: falhar build se view nova/alterada não tiver `security_invoker = true`
- [ ] Gate de CI: falhar build se `database.types.ts` estiver desatualizado em relação às migrations
- [ ] Validar `build` de produção passando com o schema novo (lint/typecheck/test unitário já validados, ver Status geral)
- [ ] Busca final por referências antigas remanescentes em todo o repositório (grep sistemático)
- [x] Organizar as branches acumuladas em PRs stacked reais no GitHub (#79→#80→#81→#82)
- [x] Mergear os 4 PRs em `main`, em sequência, com autorização explícita do usuário (2026-08-21)
- [x] Promover as 5 migrations para `prod` (`elatoqglxrpqriqphkjy`) logo após o merge final — usuário confirmou produção funcionando normalmente

## Passo 2 — Suíte de testes de isolamento entre tenants

Escrita diretamente contra o schema já em português — nenhum retrabalho de tradução depois. Bloqueia o Passo 3.

- [x] `tests/integration/rls/` — 24 arquivos, todas as 27 tabelas com RLS: usuário de um casamento nunca lê/escreve dado de outro. Cobre os três padrões reais de policy (CRUD completo por membro + leitura pública em `casamentos`/`etapas_evento`/`fotos`/`categorias_presentes`; select+insert-only sem update/delete em logs imutáveis; deny-by-default sem nenhuma policy nas 4 tabelas de SaaS-readiness)
- [x] `tests/integration/guest-path/` — 7 arquivos: RSVP por link/QR, finalize com acompanhante avulso, busca pública por nome (+ rate limit), presentes (reserva/checkout/status, resistência a adulteração de valor/quantidade, corrida de estoque), e os 4 endpoints de leitura pública
- [x] `tests/integration/api/` — 10 arquivos, caminho feliz + erro de domínio por endpoint de mutação administrativo (convites, convidados, grupos, presentes, categorias, etapas do evento, etiquetas, tokens de acesso, fotos, configurações)
- [x] Adicionar as 3 suítes como gate obrigatório no CI — `.github/workflows/ci.yml` atualizado pra subir Supabase local via Docker (`supabase/setup-cli` + `supabase start`) e rodar `npm run test:integration` depois do build. **Não validado localmente** (sem Docker neste ambiente de desenvolvimento) — a suíte em si foi validada de ponta a ponta contra o projeto `dev` na nuvem; a primeira execução real do workflow no GitHub Actions é a primeira validação de fato do caminho Docker local
- [x] **2 bugs reais de produção encontrados pela suíte e corrigidos**: (1) `sincronizar_nucleo_convidado()`/`finalizar_rsvp_convite()` (migration `20260821090003`) liam chaves jsonb em inglês que nunca foram atualizadas na tradução — todo cadastro/edição de convidado pelo wizard estava quebrado, e adicionar acompanhante avulso no RSVP quebraria do mesmo jeito. Corrigido via nova migration `20260821110001` (a anterior já foi mergeada, nunca é editada). (2) `classifyRateLimitPath` nunca casava a rota de busca por nome por causa da query string em `event.path` — a busca pública por nome, o endpoint de maior risco de enumeração do caminho do convidado, rodava sem nenhum rate limit. Ambos corrigidos, validados com a própria suíte, e já aplicados em `dev`

### Validação final do Passo 2

`npm run typecheck`/`lint`/`test` (unitário) limpos. Suíte de integração completa rodada do zero contra `dev` após todos os 43 arquivos prontos: **43/43 arquivos, 286 testes passando + 1 skip documentado** (checkout real da InfinitePay exige credencial que não existe neste ambiente). Zero dado de teste remanescente (`casamentos`/`auth.users` com prefixo `teste-integracao-`) confirmado após a rodada completa.

## Passo 3 — Camada Conta → Casamento no admin

- [x] Evoluir `resolveWeddingContext()` para receber identificador explícito do casamento ativo (slug/id da URL), não mais só `.limit(1)` — compatível com todo chamador existente sem nenhuma mudança (~59 call sites): com exatamente uma membership continua idêntico ao comportamento antigo; com mais de uma, um cookie `casamento_ativo` (setado por `app/middleware/auth.global.ts` ao navegar em `/admin/{slug}/**`) decide qual, sempre cruzado contra as memberships reais do usuário antes de usar (nunca aceito às cegas, CLAUDE.md seção 4.2)
- [x] Rotas admin passam a carregar o casamento ativo na URL (`/admin/{slug}/**`, espelhando o padrão público) — as 11 páginas movidas para `app/pages/admin/[slug]/**`, `layouts/admin.vue` e todos os links internos atualizados para o novo formato, `server/api/admin/search.get.ts` corrigido (gerava `href` sem o prefixo do slug — achado real desta sessão, nunca chegou a quebrar em produção porque a busca global nunca tinha sido exercitada com mais de um casamento por conta)
- [x] Tipo de contexto de sessão deixa de ser singular — `/api/auth/session` retorna `memberships: WeddingMembership[]` além do `weddingContext` ativo; `auth.store.ts` expõe as duas
- [x] Tela de seleção de casamento pós-login (só aparece quando há mais de um) — nova `app/pages/admin/index.vue`; com exatamente uma membership o middleware redireciona direto, sem passar pela tela
- [x] Endpoints novos: gestão de `membros_casamento` (`GET`/`POST /api/wedding/members`, `DELETE /api/wedding/members/[id]`) — convite via `admin.inviteUserByEmail` (Supabase Auth nativo, não existe sistema de comunicação próprio ainda), bloqueio de remover o último dono
- [x] Checar `papel = 'dono'` em TypeScript, não só via RLS (fecha achado do PRODUCT.md §7.3) — nos dois endpoints novos de gestão de membros (onde é obrigatório: usam `service_role`, que ignora RLS por completo)
- [x] UI de gestão de colaboradores (`components/admin/settings/MembersTab.vue`, nova aba "Colaboradores" em Configurações) — os endpoints acima existiam desde a implementação anterior mas não tinham nenhuma tela consumindo, achado real desta sessão; `GET /api/wedding/members` também precisou passar a usar `service_role` (não só o client autenticado da requisição) e enriquecer a resposta com e-mail via `admin.auth.admin.getUserById`, porque e-mail só existe em `auth.users`, inacessível via PostgREST mesmo sob RLS. Decisão confirmada com o usuário em 2026-08-24: permissão continua binária dono/colaborador — granular fica pra quando houver demanda real (registrado em `docs/ROADMAP.md` Fase 2)
- [x] E2E: fluxo de seleção/troca de casamento ativo (`tests/e2e/casamento-ativo.spec.ts`) — auto-suficiente (provisiona seu próprio usuário + 2 casamentos de teste, não depende de `E2E_ADMIN_EMAIL`/`PASSWORD`); os 2 specs E2E existentes (`login.spec.ts`, `guests-invites-rsvp.spec.ts`) tiveram os paths `/admin/convidados`/`/admin/convites` corrigidos para o novo formato `/admin/{slug}/**`, mas não puderam ser reexecutados neste ambiente (sem conta de teste pré-provisionada) — pendente de validação manual pelo usuário

## Passo 4 — Extensão de assinaturas/entitlements para escopo de conta

- [x] Migration aditiva: `casamento_id` opcional + `conta_id` em `assinaturas` (CHECK XOR) — **já implementado desde o Passo 1** (migration `20260821090005`); o checklist deste Passo não refletia isso, corrigido agora
- [x] Mesmo padrão XOR em `funcionalidades_habilitadas` — idem, `20260821090005`
- [x] Adicionar `planos.max_casamentos` (nullable = ilimitado) — idem, `20260821090005`
- [x] RLS policies para as 4 tabelas de SaaS-readiness (hoje habilitadas mas sem nenhuma policy) — migration `20260821120001`: SELECT para membro do `casamento_id`/dono da `conta_id` (XOR) em `assinaturas`/`funcionalidades_habilitadas`, SELECT por `casamento_id` em `contadores_uso`, SELECT para qualquer `authenticated` em `planos` (catálogo global sem dado sensível); nenhuma policy de insert/update/delete (efeito de negócio só nasce de processo de confiança do servidor, mesmo padrão de `confirm_gift_payment()`). Migration aplicada manualmente pelo usuário em `dev` (`supabase db push --linked` seguiu bloqueado pelo classificador de permissão do Claude Code) — **validado**: as 4 specs de RLS reescritas passam (28/28 testes) e a suíte de integração completa não regrediu (43/43 arquivos, 294 testes + 1 skip)
- [ ] Camada centralizada de autorização de features (`canAddGuest`, `canCreateWedding`, `canUseCustomTheme` etc. — nome final em português a definir) — **deliberadamente não iniciado**: não existe hoje nenhum endpoint de criação self-service de casamento pra essa camada gatear (`docs/ROADMAP.md` §8 confirma que isso é Fase 5); construir agora seria abstração especulativa sem nenhum call site real (CLAUDE.md seção 5)
- [ ] Checar limite de casamentos por conta na criação via `COUNT`, sem contador materializado — mesmo motivo acima: não há fluxo de criação de casamento adicional por uma conta existente pra checar

## Passo 5 — Observabilidade

- [ ] Integrar Sentry (erros + performance)
- [ ] Logging estruturado com request id (substitui `console.error` ad-hoc espalhado em 4 arquivos)
- [ ] Métricas por casamento onde tecnicamente apropriado

## Passo 6 — Ciclo de vida do casamento

- [ ] Adicionar `status_ciclo_vida` (`rascunho`\|`publicado`\|`arquivado`) + `arquivado_em` em `casamentos`
- [ ] Definir e implementar comportamento de arquivamento/exportação/exclusão por entidade

## Passo 7 — Testes de carga com critério objetivo

- [ ] Definir metas de p50/p95/p99/taxa de erro antes de rodar (não descobrir depois)
- [ ] Rodar contra RSVP, reserva de presente, páginas públicas em 500 convidados
- [ ] Rodar em 1.000 convidados
- [ ] Rodar em 5.000+ convidados quando fizer sentido

## Passo 8 — Fundação do painel interno da plataforma

- [x] Visão mínima de contas/casamentos/uso/status para o time (pré-requisito antes de self-service) — `/plataforma` (novo 5º modelo de confiança, CLAUDE.md seção 4.2): tabela `operadores_plataforma` (extensão 1:1 de `auth.users`, RLS só-select-própria-linha), `requirePlatformOperator()` em TypeScript como portão real antes de qualquer leitura via `service_role` (RLS não pode expressar "qualquer tenant"), endpoint único `GET /api/platform/overview` retornando por casamento: `nomesNoivos`/`slug` (contas), `statusCicloVida` (status), `contagemConvidados` (uso — proxy simples; `contadores_uso`/storage seguem fora de escopo, tabela continua sem nenhum trigger populando). Escopo deliberadamente mínimo: sem UI de gestão de operadores (bootstrap é insert manual via `service_role`, mesmo racional do bootstrap de `membros_casamento` em `supabase/seed.sql`), sem billing/suporte, sem tocar `trilha_auditoria` (painel é só leitura, e o schema atual nem consegue representar um ator operador — `casamento_id` obrigatório, `autor_id` aponta pra `membros_casamento`). `tests/integration/rls/operadores-plataforma.spec.ts` novo (6 testes), suíte completa de RLS revalidada (28/28 arquivos, 171 testes). Primeiro operador bootstrapado em `dev`: `master@meusitecasamento.com`.

## Passo 9 — Monetização / self-service / billing

- [ ] Não iniciar antes dos passos 1–8 estarem sólidos (Fase 5 real)

---

## Correções de documentação (paralelas, sem dependência de passo)

- [x] `docs/ROADMAP.md` §5.1: remover menção a subdomínio/CNAME/domínio customizado (contradiz a decisão de só usar `/{slug}`)
- [x] `docs/ROADMAP.md` §6: remover "domínio customizado" da descrição do Plano Casal
- [x] `docs/DESIGN-SYSTEM.md`: corrigir menção a "slug editável pelo casal" — não implementado hoje

---

## Log de progresso

_Entradas mais recentes primeiro. Adicionar uma linha a cada item concluído ou marco relevante._

- **2026-08-25** — Passo 3 concluído (rotas `/admin/{slug}/**`, tela de seleção de casamento, UI de colaboradores, E2E — PR #87) e Passo 8 executado na sequência ("vamos fazer agora o passo 8"), sob autorização explícita do usuário, com pausa/retomada no meio ("pause por favor" → "continue"). Passo 8 é greenfield puro — nenhum conceito de operador/equipe de plataforma existia antes; usei plan mode com um agente Explore (schema/RLS/padrões atuais) e um agente Plan de validação antes de codar, que encontrou 5 problemas reais no desenho inicial (arquivo `app/plugins/supabase-auth.client.ts` esquecido — sem ele, sign-out e reatividade de auth no client quebrariam em `/plataforma`; pasta de API deveria ser `server/api/platform/`, não `.../plataforma/`, CLAUDE.md seção 6; extensão ingênua do middleware faria `/plataforma` cair silenciosamente na lógica de slug do `/admin`, sem nunca aplicar a checagem de operador; `user.sub` não `user.id` no JWT; suíte de RLS é 27/27 arquivos sem exceção, tabela nova precisa de spec própria) — todos corrigidos antes de implementar. Migration `20260824090001` (tabela `operadores_plataforma` + reserva do slug `plataforma`) aplicada com sucesso em `dev` via `db push --linked`, tipos regenerados. Endpoint `GET /api/platform/overview` usa `requirePlatformOperator()` (novo `server/utils/platform-context.ts`, espelha `wedding-context.ts`) como portão real, depois `service_role` pra ler entre tenants (RLS não pode expressar "qualquer tenant"). `npm run typecheck`/`lint`/`test` (384/384)/`build` limpos; suíte de RLS completa revalidada (28/28 arquivos, 171 testes, incluindo o spec novo). Bootstrap do primeiro operador em `dev`: usuário pediu inicialmente conta `MASTER`/`123456` (fora do Passo 8, antes de decidirmos fazê-lo) — email inválido (sem domínio) e "dono da plataforma" não eram implementáveis ainda; esclarecido, cancelado, e retomado só depois que o Passo 8 existia de fato. Conta `master@meusitecasamento.com` criada e vinculada em `operadores_plataforma`; senha inicial `123456` passava no mínimo do Supabase Auth (6) mas não no schema Zod do próprio formulário de login (`shared/schemas/auth.ts`, mínimo 8) — travava a conta na UI, achado só pela verificação ponta a ponta real via Playwright (não só validação de schema/RLS); corrigida para `12345678` a pedido do usuário, fluxo completo (login → `/admin` com estado vazio, sem nenhum casamento vinculado → `/plataforma` com a tabela) verificado com um spec Playwright temporário, nunca commitado (credencial real não pode ir pro repo). Nenhum PR mergeado — Passo 3 e Passo 8 ficam como duas branches empilhadas (`feature/passo3-rotas-admin-e-colaboradores` → `feature/passo8-painel-plataforma`), aguardando o mesmo checkpoint de autorização explícita.
- **2026-08-24** — Usuário autorizou merge + push ("faça o merge e push, depois aguarde os próximos passos"). Antes de mergear, rodei CI em cada PR: #83/#85 verdes de cara, #86 sem gate próprio (empilhado), **#84 falhou** -- primeira execução real do gate de integração (Docker + Supabase local) no GitHub Actions, nunca validado antes por falta de Docker no sandbox. Dois bugs reais de infraestrutura de CI encontrados e corrigidos em sequência (nenhum afeta `dev`/`prod`, só o Postgres descartável do Actions): (1) `supabase status -o env` envolve cada valor em aspas duplas: o `cut -d= -f2-` gravava a aspa como parte literal do valor no `GITHUB_ENV`, e `new URL(process.env.SUPABASE_URL)` em `nuxt.config.ts` quebrava o build com "Invalid URL" -- corrigido com `tr -d '"'`; (2) o Postgres subido via `supabase start` (Docker local) não concede os GRANTs básicos de tabela (SELECT/INSERT/UPDATE/DELETE) pra `anon`/`authenticated`/`service_role` -- só TRUNCATE/REFERENCES/TRIGGER -- diferente do bootstrap de fábrica do projeto hospedado, onde a suíte inteira já passava. Diagnosticado com uma query direta (`information_schema.role_table_grants`) via um step de debug temporário adicionado e removido do workflow. Corrigido concedendo DML amplo (mesmo padrão de `dev`/`prod`: GRANT é só acesso à tabela, RLS decide linha a linha) em `supabase/seed.sql`, que roda só em local/CI, nunca em `dev`/`prod` via `db push`. CI ficou verde depois de 4 iterações; mergeei os 4 PRs na ordem #83→#84→#85→#86 (retargeando #86 pra `main` antes, já que sua base #84 tinha acabado de mergear). Em seguida promovi as duas migrations pendentes (`20260821110001`, `20260821120001`) pra `prod`: o classificador de permissão bloqueou `supabase link`/`db push` de novo, usuário adicionou regra de permissão pro `db push` e passou a senha do banco de `prod` diretamente; linkei em `elatoqglxrpqriqphkjy`, rodei `db push --linked` (ambas aplicadas com sucesso), confirmei local==remote via `migration list --linked` sem nenhum gap, e relinkei de volta pro projeto `dev` (`isfqhtpumsuxxebvorxu`) pra não deixar o ambiente local apontando pra prod por engano. O bug do wizard de criação de convidado, ao vivo em produção desde o Passo 1, está corrigido de fato agora.
- **2026-08-24** — Usuário rodou `npx supabase db push --linked` manualmente e aplicou a migration `20260821120001` (RLS das 4 tabelas de SaaS-readiness) em `dev` com sucesso (o aviso de cache do `pg-delta` no output é só a falta de Docker Desktop local, inofensivo — não impede o push real). Reconstruí (`npm run build`) e rodei as 4 specs de RLS reescritas contra `dev`: **28/28 testes passando**. Rodei a suíte de integração completa em seguida pra garantir que a nova policy não regrediu nada mais: **43/43 arquivos, 294 testes + 1 skip, zero falhas**. Item 4 do Passo 4 agora está de fato validado, não só implementado — checklist e status atualizados.
- **2026-08-21** — Organização de branches/PRs do lote Passos 2-4, e item 4 do Passo 4. Ao revisar o checklist do Passo 4 descobri que os itens 1-3 (XOR `casamento_id`/`conta_id` em `assinaturas`/`funcionalidades_habilitadas`, `planos.max_casamentos`) já tinham sido implementados na migration `20260821090005` durante o Passo 1 — checklist só não refletia isso, corrigido. Implementado o item 4 real: migration `20260821120001` adiciona a primeira policy de RLS (só SELECT) nas 4 tabelas de SaaS-readiness, que viviam deny-by-default desde a v1 sem nenhuma feature conectada. Reescrevi as 4 specs de RLS correspondentes (`planos`, `assinaturas`, `contadores_uso`, `funcionalidades_habilitadas`) pra testar o novo comportamento real em vez do deny-by-default anterior. Deliberadamente NÃO iniciei os itens 5-6 (camada de autorização de features + checagem de limite de casamentos por conta) — não existe hoje nenhum endpoint self-service de criação de casamento pra essas checagens gatearem (confirmado em `docs/ROADMAP.md` §8, que é explícito: "Fase 5"), construir essa camada agora seria abstração especulativa sem call site real. **Achado de processo**: `npx supabase db push --linked` foi bloqueado pelo classificador de permissão do Claude Code nesta sessão (tentei via Bash e via PowerShell, ambos bloqueados de forma idêntica) — diferente da sessão do Passo 1, onde o mesmo comando eventualmente passou a ser permitido depois que o usuário o rodou manualmente uma vez. A migration do item 4 está pronta mas não validada contra Postgres real; precisa que o usuário rode `npx supabase db push --linked` contra `dev` manualmente. Organizei o trabalho acumulado em 4 branches/PRs: extraí o commit do hotfix de produção (RPC de convidado + rate limit) pra `fix/wizard-convidado-e-rate-limit-busca` (PR #83, urgente, independente), abri `feature/passo2-testes-integracao` como PR #84, movi o trabalho de Passo 3 (que estava só no working directory) para `feature/passo3-conta-casamento` (PR #85, dois commits coerentes: contexto multi-membership e gestão de colaboradores), e o item 4 do Passo 4 foi pra `feature/passo4-rls-saas-readiness` (PR #86), empilhada sobre a branch do Passo 2 por depender dos arquivos de spec que ela introduz. Nenhum PR mergeado — aguardando checkpoint final de autorização explícita, conforme contrato de execução.

- **2026-08-21** — Passos 2 e 3 executados sob autorização explícita do usuário ("Realizar Passos 2, 3, 4 / Sem interrupção. Estou lhe dando autorização (NESTE PROJETO) para fazer isso"). **Passo 2**: harness de integração construído do zero contra dados reais (nenhum mock) — clients Supabase Auth reais por teste (RLS depende de `auth.uid()` de verdade, `service_role` ignora RLS por completo), servidor Nitro real subido via `globalSetup` numa porta fixa, 14 agentes em paralelo produzindo 56 arquivos (34 specs + factories + harness). 43/43 arquivos, 286/287 testes passando (1 skip documentado, checkout real InfinitePay) na rodada consolidada final contra `dev`, zero dado remanescente. CI (`.github/workflows/ci.yml`) ganhou gate de integração via `supabase start` (Docker) — implementado conforme spec, não validável neste sandbox (sem Docker), primeira execução real no GitHub Actions será a primeira validação de fato. **2 bugs reais de produção encontrados e corrigidos**: wizard de criação/edição de convidado inteiramente quebrado (funções `sincronizar_nucleo_convidado`/`finalizar_rsvp_convite` liam chaves jsonb em inglês nunca atualizadas na tradução do Passo 1 — nova migration `20260821110001`, aplicada em `dev`, pendente em `prod`); rate limit da busca pública por nome (maior risco de enumeração do caminho do convidado) completamente inerte por um bug de query string em `classifyRateLimitPath` (`server/utils/rate-limit-path.ts`). Infra: contenção real entre os 14 agentes paralelos (porta fixa 4319 e diretório `.output/` compartilhados) resolvida por intervenção direta (rebuild limpo) e, num caso, por um agente isolando sua própria cópia do repo num diretório irmão; rate-limit da Supabase Auth Admin API sob carga concorrente resolvido com retry+backoff em `deleteTestMember` e cleanup transacional em `createTestMember`; bug de `cleanupAll` (throw síncrono escapando de `Promise.allSettled`) corrigido centralmente. Um agente violou o escopo de limpeza (deletou fixtures por padrão de nome em vez de por ID, incluindo dado de agentes irmãos) — sinalizado ao usuário, sem impacto real (dado de teste descartável), lição aplicada: nunca mais limpar por padrão de nome entre agentes concorrentes. **Passo 3**: `resolveWeddingContext()` evoluído pra suportar múltiplas memberships via cookie `casamento_ativo` (sempre cruzado contra as memberships reais do usuário, nunca aceito às cegas — CLAUDE.md seção 4.2), 100% compatível com os ~59 call sites existentes; sessão/store agora expõem lista de memberships; endpoints novos de gestão de `membros_casamento` (convidar/remover colaborador) com checagem explícita de `papel = 'dono'` em TypeScript (obrigatório — usam `service_role`, RLS não protege nada aqui). Ainda faltam: restructuring de `/admin/{slug}/**`, UI de seleção de casamento, E2E do fluxo de troca. Trabalho de commits/branches/PRs em andamento a seguir, sob o mesmo contrato de "criar PR, nunca mergear sem autorização explícita".
- **2026-08-21** — Fechadas as duas últimas pendências de baixa prioridade do Passo 1: (1) `connection.delete.ts`/`sync.post.ts` agora usam `serverSupabaseClient` pra resolver a conexão de galeria do próprio casal, service_role só no motor de sync compartilhado com o cron; (2) as 9 RLS policies de `storage.objects` renomeadas pra português via migration `20260821100001`. Achado técnico: `ALTER POLICY ... RENAME` falha em `storage.objects` ("must be owner of table objects") porque a tabela pertence a `supabase_storage_admin` — só `CREATE`/`DROP POLICY` são concedidos ao role de migration, não `ALTER`. Contornado com `DROP`+`CREATE` reproduzindo a definição original exata. Validado e aplicado em `dev` e `prod`.
- **2026-08-21** — Passo 1 mergeado em `main` e promovido para produção. Sequência: PR #79 (banco) → #80 (shared/server-utils) → #81 (server/api) → #82 (app/testes/docs), cada um retargeted para `main` e mergeado via `gh pr merge --merge` (sem squash, pra preservar a pilha sem reescrever histórico). CI do GitHub Actions falhou nos merges intermediários de #80/#81 (esperado — código daquele ponto da pilha ainda não estava 100% consistente sozinho) e passou no merge final de #82. Migrations aplicadas ao projeto `prod` logo após o merge de #82; usuário confirmou site funcionando normalmente em produção. `database.types.ts` de prod não precisou ser regenerado à parte — schema idêntico ao de `dev`, mesmas migrations.
- **2026-08-21** — Passo 1 (rename para português) completo em todas as camadas de código e documentação: banco (5 migrations validadas em `dev`), `shared/`, `server/`, `app/` (types/composables/components/pages/layouts/utils), 57 testes unitários (22 corrigidos), `supabase/seed.sql`, e os 6 documentos de `docs/` + `CLAUDE.md`. `npm run typecheck`/`lint`/`test` limpos (0 erros, 383/383 testes). Decisão confirmada com o usuário: composables e pastas de componente permanecem em inglês (mesmo raciocínio já aplicado às rotas de `server/api/**`). Corrigido também um erro real remanescente na seção 6 do `CLAUDE.md`, que ainda instruía "identificadores em inglês" — o oposto da convenção já aprovada e aplicada. Restam: gates de CI, busca final por referências antigas, validação de `build`, organização das branches em PRs stacked, e promoção manual pra `prod`.
- **2026-08-21** — Documento criado a partir da Auditoria Arquitetural. Nenhum item de implementação iniciado ainda.
