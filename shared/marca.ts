/**
 * O nome do produto, num lugar só.
 *
 * Estava escrito à mão em seis arquivos — SEO, rodapé do site, título de erro,
 * fallback da barra do convidado —, e foi assim que a remodelagem anterior
 * deixou ocorrências antigas para trás. Rebrand é uma troca de string, e uma
 * troca de string só é segura quando existe uma string.
 */
export const NOME_DO_PRODUTO = 'MeuSiteCasamento'

/**
 * O símbolo da marca: o "&" do convite.
 *
 * Não é uma sigla ("MSC" não significa nada para quem lê) nem arte desenhada —
 * é o mesmo caractere que a capa do site usa como ornamento, e o único
 * desenho que um produto de casamento não precisa explicar. Sai sempre em
 * `font-display`, então acompanha a serifada do contexto em que aparece.
 */
export const SIMBOLO_DA_MARCA = '&'
